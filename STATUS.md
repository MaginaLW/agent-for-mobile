# 项目状态

> 每次会话收尾更新。保持 ≤ 20 行。

- **当前阶段：批次 1（干掉每跑一腿的人工前后置）二次验收通过，三腿一次连跑完成**（V2352A/Android16，commit `337113c`）。`20260801T143739-ff105d203a35` 一个 run 跑完 Allow→Stale→Deny，`status=passed`、退出码 0、`cleanup.ok=true`；三腿 `teardown.verdict` **均为 `clean`**，人只点了 3 次确认卡、中途一次框都没清。`confirmation` 依次 allowed/allowed/**denied**，`safety_code` 依次 OK/`E_STALE_REF`/`E_BLOCKED`，`dangerous_calls` 各 1、`input_evidence_matched` 各 true、`card_visible` 各 true。冷启动自举另由零 token 脚本 `p0-foreground-bootstrap-check.ps1` 单独验过：**重绑前 `event` → 重绑后 `bootstrap`，package 级无 activity，退出码 0**。
- **首次验收失败的教训（08-01 10:17，同一批次前一个 commit `899c095`）：判据必须能把"场景没构造成功"和"通过"分开。** 那轮 tracker 自举三次 `-Provision` 全程 `foreground_identity_source=event`、`bootstrap` 零次出现——**自举分支压根没被执行到，而失败形态与通过长得一模一样**（没有 `identity_unset` 卡死是事实，但不能记在自举头上）。修法不是调参数，是给"未触达"一个自己的名字：新脚本四态 `passed`/`not_reproduced`/`unavailable`/`failed`，退出码即结论。这是"用例把错误行为钉成预期"的同族风险第三次出现。
- **Deny 腿第一次上真机就抓到一个"最像成功"的假通过**：网关 debug 测试控制白名单只有 allow/stale、漏了 deny，`press_key` 直接回 `E_BLOCKED("debug 测试腿不在白名单", channel=test-control)`——**确认卡根本没弹**，31 秒结束。而 `E_BLOCKED` 恰是该腿的预期错误码，执行器照常报告"符合预期"。**拦住它的是 runner 独立读 app 私有文件里的 `confirmation`**（无真人决定即判失败）。这把「Deny 四条判据全部来自被测组件自报」从理论顾虑变成实锤。白名单已放开并补正反用例——**此前的用例把 `leg="deny"` 当"不支持的腿"，正在保护这个 bug**（同一类错误第二次：用例把错误行为钉成预期）。
- **Deny 腿带外验证本轮手工做了一次**：经 runner 自己的 adb 通道截屏，marker `P0DENY-…` **原封不动留在输入框里**——微信发送后会清空输入栏，文字还在即发送没发生。注脚：那张截图消息列表往上滚了，"消息区无该 marker"**未被视觉证实**，扛结论的是输入框这条正证据。自动化仍待补，manifest 现仍如实记 `gateway_reported_blocked_no_independent_check`。
- **Allow 腿卡五轮的真因：`press_key` 用未过滤的焦点节点走 a11y 通道，把 IME 通道短路了。** `viaNode` 直接对 `findFocus(FOCUS_INPUT)` 调 `ACTION_IME_ENTER`；微信会话页那个残留节点既不 focused 也不 editable，却**接受该动作并返回 true**（假成功），`viaNode || ImeBridge.enter()` 当场短路。**铁证**：同一调用序列里 `type_text` 报 `ime_commit_ocr`（无节点通道）而 `press_key` 报 `a11y_ime_enter`——两个工具对"有没有可用节点"的判断互相矛盾。这是《残留焦点节点连累三处》的**第四处**，而那条教训原文就是"要把所有取用该节点的路径一起改"。已统一到 `FocusedInputSnapshot.nodeUsableForAction`（只认 `IdentitySource.A11Y`）。修好后通道变为 `editor_action:send`——真正让它生效的是第 2 轮那条被挡住没执行到的修复：`ImeBridge.enter()` 原按「是不是多行」二选一且接反，安卓的约定是看 `NO_ENTER_ACTION`+actionCode，不看 multiLine（`EnterStrategyTest` 6 条用例钉住微信实测契约）。
- **方法论（最值钱的一条，已进 knowledge）：修不动的时候先加可观测性，别加假设。** 第 2 轮改完 EnterStrategy、结果照旧，而返回里没有通道信息，**连"修的地方有没有被执行到"都验证不了**；第 3 轮不再加假设，只给危险动作加了 `enter_channel`（走哪条路 + 输入框契约，成功失败都带），当轮定位真因。「回车开关」那条假设也在第 1 轮被证伪。
- **危险动作的返回值必须由后验决定**：`performEditorAction` 只要连接活着就返回 true，微信不理会时照样"成功"。已加发送后验（**只判定、绝不换通道重试**——后验失败时可能其实已发出，重试等于冒重复发送）。后验自身还踩过一次假阳性：基线用了 Enter 前那次 OCR 读回的噪声串，已改用已提交文本原文。
- **后验为三态 `sent/not_sent/unverified`**：布尔版把"判不了"当"没发出去"，最要命的是**>64 字的消息即使真的发出去也必然报失败**（基线取自 64 字上限的 `preview`）；测试 marker 18 字恰好绕过，离线真机都测不到。`unverified` 按 ok 返回并要求只读复核；判据 a11y 优先、OCR 降级。runner 侧只禁矛盾（`not_sent`），正证据由 `ui_find` 承担——强求网关自证会让 P0 **永远过不了**（微信 OCR-only 链下发送成功后输入栏本就是空的）。
- **腿间约束与预检两道重试（本轮逼出来，已进 runbook）**：①被拦下的腿按定义把 marker 留在输入框、键盘还支着，而预检候选区是**固定几何**，键盘顶起布局后那块地方量的是键盘——`empty` 会给出**看似为空的错误结论**（`输入法窗口占屏` 这道守卫拦得对）。**已由腿末 teardown 自动做掉（08-01 二次验收实证三腿全 `clean`）；判别式不是键盘状态而是微信在不在前台**——首次验收里 Stale 腿停在桌面、28 次退格打给了桌面，而 Deny 腿 `keyboard` 同为 `already_hidden` 却 `clean`，这条只差一个变量的对照直接钉死了真因。现在 teardown 动手前先确认前台并拉回，拉不回则一个键都不发并报 `skipped_not_foreground`。②`-ReadyRetries` 只在拿到**错误信封**时重试，覆盖不到"答得成功但内容没稳"（`ok=true, empty=true, probe_ready=false`）：provision 后一次硬失败、两分半后同一设备没人碰手机直接通过。已加 `-NotReadyRetries` 并把 `attempts`/`waited_ms` 带进输出，下次自己会说话。
- **台账归因按 channel 区分 `E_BLOCKED`**：`channel=overlay` 是真人拒绝（`safety-denied`，安全门尽职），`channel=test-control` 是测试控制拒收（`test-control-blocked`，卡根本没弹）。两者此前并排记成同一个 `safety-denied`——归因列存在的全部意义就是区分它们。历史那一行已更正。
- **harness 三处摩擦已修**：①派单锁按"能否独占打开"判活，崩溃残锁自动清；②`--allowedTools` 只是免确认名单——执行器真在本机跑起了 `Bash`，已补 `--disallowedTools`，越权扫描改为**失败路径上也跑**并写进 manifest；③开跑前零 token 预检（R 级 `p0_probe_region_state`，判据与宏共用同一实现），残留文字与"没停在会话页"都在派单之前拦下。
- **harness 判据五处修复（都由真机逼出来）**：①执行器调 `ReportFindings` 被越权扫描判死，**且发生在真人已允许、危险动作已执行之后**；②marker 归一化未做 O→0，把一次真正成功的发送判成"证据不匹配"；③"marker 在消息区"判据依赖 a11y 焦点几何，**在微信上结构性不可满足**，改为焦点几何结构性缺失时用设备自报的输入栏候选区划线；④零 token 预检在 `-Provision` 后必失效，加就绪重试；⑤终态报告不认 markdown 粗体——**而 dispatch 同一处会把 `**结果：失败**` 落进兜底分支记成 success**，台账层面最危险的错分，模式已收进 `dispatch-ledger.ps1` 由两处共用。
- **执行器工具面两次咬人，已按类补严**：`--allowedTools` 只是免确认名单，真正限制的是 `--disallowedTools`，而它是手写枚举。2026-07-26 `ToolSearch` 被越权审计误杀；07-31 执行器调 `ReportFindings` 把整腿判死。同轮发现 **`PowerShell` 一直没禁**（只禁了 `Bash`，而本机是 Windows，仓库里放着 gateway token）。现按类分组并加离线断言（两个 shell 必须成对）。详见 [brain/harness.md](docs/knowledge/brain/harness.md)。
- **仓库整改（2026-07-26~27，8 次提交）**：A 仓库卫生 + **危险动作提示词从 1204 行 PowerShell 收进 `scripts/tasks/*.tmpl.md`**（黄金回归逐字钉住）· B 一键校验 `scripts/check.ps1` · C 发送后验三态 · **跑测环境真因**（假 adb 里 `find` 被 Git Bash 的 Unix find 顶替、递归扫 C 盘到 30s 超时；此前误诊成机器负载与 stdin 各一轮）· D 凭据与审计加固 · E Deny 腿接进 runner · F 台账 `fail_reason` 列。**剩余 F**：审计目录迁 filesDir（**需真机验证 `run-as` 能读该路径**）、套件提速、`ToolRegistry.callInternal`（179 行，全仓最长）。
- **子代理复查（2026-07-27）修掉 9 处，其中两处是当轮核心改动自身的缺口**：①`fromOcrReadback` 把"OCR 一个字没读到"判成已发送——**换了触发条件的谎报成功**，且被我自己的用例固化成预期行为；②Allow 腿"必须 `sent_verified=true`"的断言写反；③Deny 提示词与严格签名自相矛盾；④后验预算 2s→6s；⑤a11y 腿补身份校验；⑥`preferredBounds` 补 ≥32 守卫；⑦TokenStore 落盘失败不再静默缓存；⑧`Stop-DevEnvGradleDaemon` 全机杀改显式 opt-in；⑨多处夸大的注释/manifest 字段改成如实措辞。
- **OCR 融合判定抽成纯函数 [OcrFusionPolicy.kt](app/gateway/src/main/java/dev/magina/gateway/a11y/OcrFusionPolicy.kt)（15 条离线用例）**：规则原先埋在 `snapshot()` 的 55 行循环里，改一处要烧一轮真机。**两次更正（同一度量错误犯了两遍）**："`snapshot()` 317 行"与"`gatedOnly` 232 行"都是错的——两次都把"到下一个 `fun` 的距离"当函数长度。用括号匹配重测：全仓最长是 `ToolRegistry.callInternal`（179），其次 `snapshot`（130），**没有任何 200 行以上的函数**。
- **工序按"是否消耗人的精力"分三道并行（2026-08-01 建立，[docs/backlog.md](docs/backlog.md)）**：A 独立闭环 / B 你一个决定（文字，不碰手机）/ C 你在真机旁。改动堆成**验收批次**、钉 commit SHA，把人的参与从"每个改动一次跑测"降到"一批一次"；三会话各跑一个 worktree，故队列写权收归主会话、工序会话经 `git show main:` 读，**干完主动 `send_message` 叫醒主会话**（此前靠用户说一句"读进展"才轮询，等于让用户当调度器）。协议踩出三个洞已补：合钉住的 SHA 而非分支 tip、台账无论通过与否都落 main、批次划分按**改动触达的表面**判而非按意图判。**最贵的一个是主会话自己踩的**：核查 B 道是否真由用户拍板时，脚本用 `tool_result` 做过滤条件排除工具回执，而 AskUserQuestion 的答案恰恰是 `tool_result`——证据被整类丢弃，于是把"我没查到"报成"它编造了用户授权"，且已落盘、已当面宣告。**判据的过滤条件正好排除了目标证据，且没做过一次正对照**；对被测组件坚持"不采信自报"，对自己的核查脚本却一次都没质疑。已更正（[backlog §7.1.2](docs/backlog.md)）。
- **批次 2（通知栏审批）与批次 3（Deny 带外验证）均已真机通过并合入 main（`f08cda2`，08-02）**。批次 2 判据 1 **收窄为「屏幕亮着但人没盯着」**——锁屏审批与 D1 结构性冲突（`requireKnownForeground` 跑在 `policy.assess` 之前，锁屏时卡与通知**都不会出现**，既有用例 `confirmerCalls == 0` 早就钉着），用户 08-02 拍板"先收窄、再做语义意图"，锁屏两条已如实归档为**从未验过、不当成通过**。通过的机械证据是 `confirmation_channel`：三腿全为 `notification`，**不采信用户口述**——这条是 A 道自补的，形态同 Deny 腿那次「判据全靠自报」。**「Deny 腿四条判据全部来自被测组件自报」从 07-31 挂到今天正式清掉**（`deny_out_of_band` 三轮 `not_sent_confirmed` 无退化）。**方向由一次搜索扭转**：连挂三轮都在调通知属性，而 `CATEGORY_CALL` 是"申请"不是"授予"、`CallStyle` 强制按钮与"允许/拒绝"结构冲突——已立为会话纪律 §3。
- **语义意图审批离线实现已成（`ed8df7b`，A 道，JVM 345 测试 +26 条），但开关默认关着——今天手机上的行为一个字没变**。主会话独立 grep 过全部 `SafetyGate(` 构造点核实：唯一生产装配 `ToolRegistry.newSafetyGate` 不传 `intentApproval`，未采信自报。A 道自己做了正对照（把 `targetLabel` 判据短路成永真 → 两个测试各红一条 → 还原后全绿），证明判据确实接在它要判的路径上。**打开开关属安全姿态变更，不由离线提交生效。**
- **「批准后重建证据」（原选项 C）已离线做完并被用户提前立项（08-03 拍板"先做重建、一次交付走开也能用"）**，因为 A 道离线查出**新腿在原实现下必然失败**：输入证据按焦点身份取，而 IME 会话 id 每次 `onStartInput` 自增哈希必变——切走再回来证据就取不出来了，**不是判据太严是证据没了**。同族第二处随后也被离线挖出：`PreparedTargetEvidence` 同为 120s TTL 且是硬要求，**只重建输入证据的话 5 分钟必然仍失败，且失败形态与今天一模一样**。现两处都重建，三态 `Rebuilt`/`Mismatch`/`Unverified`，读不出来 fail-closed，基线一律是卡上那份（sha256 + label + 包），**先验会话再验内容**。
- **意图有效期用户 08-03 拍板 5 分钟**（语义是"批准后最多隔多久回到微信"）：`foregroundWaitBudget` 30s→300s、`intentTtl`→360s、`decisionTimeout` 保持 90s，关系写成构造断言（`intentTtl ≥ foregroundWaitBudget`；等前台超过证据 TTL 则**必须装配重建通道**——该断言挂在能看见通道装没装的地方，不挂可写错的布尔）。**放宽有效期不放宽内容完整性**：执行前重读并与卡上摘要比对，主会话已独立核实。
- **批次 4 生产装配已做完（A 道，`67ab582`，08-03）：开关打开、两处证据重建接上、Stale 腿短预算 20s、确认卡 90s/通知 105s**。§9.6 那处停车按 backlog §8 的方向解决——**整条会话标题识别栈下沉到 `src/main`（`ConversationSurfacePolicy`），宏改调同一份**（debug 侧只留 typealias），退路（debug-only + 钉住 release 恒 `Unverified`）因此不需要：`ConversationSurfacePolicyTest` 在 `src/test`，**`testReleaseUnitTest` 跑得通它就是"release 里也在"的机械证据**。归一化一并下沉到 `core.TextNorm`（`OcrEngine.norm` 转发）——它此前住在 import 了 ML Kit 的类里，**JVM 用例加载不了，全仓最常用的字符规则一条用例都没有**。check.ps1 五项全绿、JVM 405 测试，正对照做过（短路两处判据 → 三条用例当场变红，还原后全绿）。
- **装配时才暴露三件事，都是"实施笔记只覆盖到写笔记的人已经想到的接线"那一族（spec §9.7）**：①题六的 OCR 归一包含判据落地了但**接线没接**——意图没带 `contentNormalized`，微信这条 OCR-only 链上重建**永远 `Unverified`**，收益在唯一的目标 App 上一次都兑现不了，而失败形态与"通道坏了"一模一样；明文必须是**全文**，拿 64 字 `preview` 当基线会重演「>64 字必然失败」（发送后验栽过，18 字 marker 恰好绕过）。②重建成功后证据得**落得回去**（`rebindApproved`，内容事实一律来自意图）。③**标题在 `judge` 里原来也是逐位相等**——与 §9.5 内容那处同一形状、写 §9.5 时没看这一行；现同为两档：a11y 严格、OCR 归一包含，**读回是已批准标签的一部分判 `Unverified`（漏识），互不包含才判 `Mismatch`**。
- **⚠️ 开关带来一处能力收窄，而本批四腿看不出来**：`resolveViaIntent` 无条件要求 `preparedTargetEvidence`，而**全仓只有 P0 准备宏会记录它**。`press_key(enter)` 本来就要求，所以送信路径没变；**危险 `ui_action` 点击开关后一律 `E_STALE_REF`**。方向 fail-closed，但**接任务 4/2 端到端链路之前必须先解决**。
- **下一步：批次 4 真机，但第四条腿的 runner 支持还没做**（A 道下一项）。`run-p0-safety-smoke.ps1` 只接受 `Allow|Stale|Deny`，缺 `p0-safety-reentry.tmpl.md`、缺 `DebugTestControl` 的 leg 白名单、缺"切走后由 runner 经 adb `am start` 把微信拉回来"那一段与对应离线用例。**在那之前只能跑既有三腿，而三腿只证明"开关打开后既有判据没退化"，证不了新腿那份收益**。现场三条防误判已进验收单：Stale 腿**几十秒终态是对的**（等满 5 分钟说明短预算没生效）、**挡住它的理由是"等前台恢复超时"而不是"包变了"**（对着 trace 错误原文核，别只看 `safety_code`）、**OCR 抖动导致的 `Unverified` 是正确的 fail-closed**。另：`-Provision` 装的是 **debug APK**，真机通过**区分不了**"功能成立"与"只在 debug 构建里成立"。随后 D3、Shizuku、IME 自动切换及任务 4/2 端到端链路。
- **遗留/障碍**：C 道在新 worktree 首跑必踩两轮空转——**runner 只装不构建**（缺 APK 直接 `setup-fail`），而 `local.properties` 是 gitignored 的、不随 checkout 过来（`SDK location not found`）；跑 runner 前**不要手动改设备状态**——手动切过 IME 会让 runner 把"原输入法"记成网关；审计目录迁 filesDir 仍未做（`run-as` 可读性已实证，迁完需复验）；S5 RemoteInput、S2 Shizuku 重启存活、`share_file` activity 级 verify 待补；Codex 订阅额度 2026-07-30 恢复（非阻塞）。
