# 项目状态

> 每次会话收尾更新。保持 ≤ 20 行。

- **当前阶段**：P0 统一硬门尚未整体通过，但 2026-07-25 把卡点一路推进到了**架构级根因**（见下）。当天又修 4 处真实缺陷，每一处都由"失败阶段前移"独立验证：①`OcrEngine` 加伽马校正（灰度→伽马→对比度拉伸 LUT，参照 Appium OCR 插件预处理链）——真机证实标题从"零候选"变为可识别；②`revision` 被状态栏时钟/网速事件污染（实测静默下每 2 秒 +3~4），导致"动作前后 revision 必须一致"的新鲜度校验几乎必败，已在 `onAccessibilityEvent` 排除 `TYPE_SYSTEM` 窗口的内容变化事件（同 knowledge #11 病根）；③Android 侧 `performValidatedFocusProbe` 的标题门槛仍是 0.65，07-24 宏侧放宽实际一直没生效（实测标题 0.59）；④`P0PreparedTargetRecorderValidator` 同时存在严格 `==` 与 0.65 两个毛病；⑤盲点安全区写死 0.84h~0.94h，在 1260×2800 上落点 y≈2492 砸在消息列表壁纸上，真实输入框实测 y≈2634~2755，已改为锚定系统底部 inset 推算并抽成可单测的共享纯函数 `p0FocusProbeRegion`。
- **架构级根因（当天最重要发现）**：在微信**已确认停在会话页、输入栏文字模式、文本框空白**的理想状态下，宏仍在 `focus_ready` 超时。超时信息逐条列出缺失条件，其中 **IME 侧三项（激活/InputConnection/focusedInputId）全部正常，只有 a11y 侧取不到焦点节点**。串起当天全部快照证据：**每次 `ui_snapshot` 的 a11y 元素永远只有状态栏那 13 项，微信会话页的标题/气泡/输入框全部来自 OCR，无一 a11y 节点**——这台设备上的微信对无障碍树基本不透明（与当天网络调研"微信 ≥8.0.52 有意混淆 a11y 节点数据对抗自动化"吻合）。即：宏要求的"a11y 可见的焦点可编辑节点"在此微信版本上**不可满足**，与点击精度、OCR、几何均无关。而 [SafetyGate.kt:76](app/gateway/src/main/java/dev/magina/gateway/core/SafetyGate.kt:76) 按设计硬性要求 Enter 前具备 a11y 侧 `focusedInputId`+`bounds` 与 IME 侧 `imeSessionId` 双命名空间证据（knowledge #43），a11y 侧拿不到 → `PreparedTargetEvidence` 无法形成 → 链路必断。**2026-07-22 那条"人工预聚焦+无 ref type_text"老路也已被此门有意堵死，不能退回。**
- **监督式 runner**：已离线实现且真机验证 provision/sensitive_entry/unrecognized_entry 均可正确通过；业务动作仍只走 `dispatch.ps1 → gateway MCP → SafetyGate → executor`。Allow/Stale 仍未真正跑到确认卡这一步。
- **IME 单命名空间降级门：已实现并真机跑通（2026-07-25）。** 第 5 次只读诊断 `macro_run(p0_wechat_file_transfer_prepare)` 首次返回 `ready:true / identity_source:ime_only / ime_session_id:ime|1a8c…/ focused_input_id:null / coordinate_fallback_used:true`——**prepared target 自 07-25 卡死以来第一次记录成功**，意味着宏、`withFreshPreparedTargetGuard`、recorder validator、证据 store 整条链在降级模式下全部通过。降级模式下 `input_focused/input_editable` 如实报 false（不谎报 a11y 证据）。**尚未验证的是其后的 `type_text` + Enter + 真人确认卡（需人在手机旁）。** 核心是新增 `FocusIdentity(source = A11Y | IME_ONLY)` 作为**唯一降级决策点**（a11y 可得必走严格链；a11y 有值但格式非法一律 fail-closed），并把身份来源持久化进两个证据 store，改造面覆盖 `UiTools`、`SafetyGate`/`SafetyPolicy`、宏就绪判据、recorder validator。顺带堵掉两个真实缺口：①`focusedInputSnapshot` 原先在无节点时把 IME 会话 id 填进 a11y 的 `id` 槽（隐式命名空间混用，此前仅靠 store 格式断言侥幸挡住）；②`typeTextNoNode` 在无 ref 时**完全跳过 OCR 读回**，降级链会永远拿不到证据，现改为退到与盲点探针同一几何的输入栏区域读回，且 IME-only 下读回未过就不记证据。确认卡在几何不可得时改展示"已降级 + IME 会话 + OCR 读回结论"，不静默少一项。**安全强度确有让步**（失去 a11y 焦点身份、bounds 及由其推出的"焦点确实是底部输入框"几何保证），已按 spec §4 落盘 knowledge。
- **真机排查中修掉的 4 处（每处都由一次只读诊断独立证伪，共 5 次派单 ≈ $1.9）**：①`findFocus(FOCUS_INPUT)` 在微信会话页返回的是**既不 focused 也不 editable 的残留节点**（不是 null），故"a11y 可用输入身份"判据必须是 **editable**，且 `focusedInputSnapshot`/`FreshPreparedInputProof` 都只在 editable 时才产出节点 id 与几何，否则证据会绑到一个打不进字的节点上；②`ui_snapshot` 补出 `focused_identity_source`；③**零 UI IME 与 `ime_visible` 恒假**（详见 knowledge，含两处既有安全门）；④"session 必须换新"判据同样不成立（微信输入框键盘收起后仍持焦点）。留下的降级判据是"会话归属 + 永不走已聚焦短路 + 输入栏 OCR 读回"。
- **2026-07-26：降级链 `prepare → type_text → OCR 读回 → 输入证据 → 后置复核` 已整段跑通**（`channel=ime_commit_ocr, committed=true, verified=true`）。为此又修 3 处，全部是**同一个"残留焦点节点"**的不同下游症状（详见 knowledge「那个残留焦点节点会连累三处」）：`type_text` 无 ref 时误把残留节点当输入节点（导致 SET_TEXT 打不进且读回用退化 bounds 裁剪，OCR 报 `at least 32!`）、读回几何用错坐标系（应以截图位图尺寸为准）、读回带太矮（文字基线在 inset 之下，需 260px 带）。另修：读回异常被 `runCatching.getOrNull()` 吞成 null。
- **Allow 腿真机结果（2026-07-26，已按证据订正）：`macro_run` ✅ → `type_text`（committed+verified）✅ → 确认卡弹出、真人点了「允许本次」✅ → 确认后复核 ⛔ `E_BLOCKED「前台 APPLICATION 身份未知」`，未发送任何内容。** 早前记的"确认卡从未弹出"是错的：确认状态确实走到了 `allowed`（只有真人点击才能推进），那次 `run-manifest.json` 记的失败是 trace 审计，与执行器 trace 里最后那条 E_BLOCKED 不是同一件事。**卡确实弹了，只是没被截进证据图**（见下条）。翻出证据图后根因也基本清楚了：自家确认卡是可获焦的 `TYPE_APPLICATION_OVERLAY`，它在时 App 窗口既非 active 也非 focused，收起后系统重新激活还要几帧，而 [SafetyGate.kt](app/gateway/src/main/java/dev/magina/gateway/core/SafetyGate.kt) 的**确认后第二次 `requireKnownForeground`** 正好撞在这几帧上。已改：`settledWindows()` 在"一个活动应用窗口都没有"这种瞬时形态下有限重读（5×80ms），**只重读不放宽判据**，窗口真没了照旧 unknown，D1 归属规则一字未动。属假说驱动，下轮 `foreground_reason` 可证真伪（应为 `no_application_window`）。
- **2026-07-26 下半场：前台身份诊断面已补齐，待一次真机取证。** 上面那次 unknown 之所以查不下去，是因为 `resolveForeground` 判 Unknown 的三条互斥路径（从未接受过窗口事件 / 无 active·focused 的 APPLICATION 窗口 / 已接受身份的 windowId 与当前活动应用窗口不一致）在返回体里长得一样——微信应用窗口 `root=null`，package-only 后备也是空串。已加：`ctx.foreground_reason`（失败那次自带原因）；`foreground_app` 扩成只读诊断（`selected_window_id`、`tracked_identity`、`windows[]` 含 **root_package**、tracker 最近 24 条窗口事件处置 `recent_events`）；Allow/Stale 任务卡加"前台身份取证例外"（unknown 时允许额外只调一次 `foreground_app` 再报失败）。**刻意只加诊断不改 tracker 判据**——两种可能（窗口列表瞬时取空 vs 应用窗口 id 真换了却没等到可接受事件）修法相反，后者要动的正是 D1 那道 overlay 防污染防线。离线：`:gateway:testDebugUnitTest` + `assembleDebug` 全绿，监督式 runner 离线 31/31。**APK 未装机，尚未真机验证。**
- **下一步（按优先级）**：①在微信里打开 设置→聊天→**使用回车键发送消息**，重跑一轮 Allow——这一轮直接判定 P0 能否整体通过（安全门已在 17:21 端到端跑通，缺的只有"消息真的发出去"这一条后置）；②若开开关后仍不发，再谈通道决策（让 IME 显示极简输入视图逼出发送按钮 / 换一个对回车有反应的目标验证发送后验 / 等 Shizuku 注入真实按键）；③冷启动自举（服务重启后 `foreground_reason:identity_unset`，本轮已活体复现）。随后 D3、Shizuku、IME 自动切换及任务 4/2 端到端链路。
- **harness 摩擦已修两处（2026-07-26，离线 dispatch 17/17 + runner 33/33）**：①派单锁改为"能否独占打开"判活（`scripts/lib/dispatch-lock.ps1`），崩溃残锁自动清一次，仍被持有则照拒；②执行器权限面——实测 `--allowedTools` 只是免确认名单，执行器真在本机跑起了 `Bash`，已补 `--disallowedTools` 禁掉 shell/文件/Web/子代理，并把"有没有越权调用非 gateway 工具"抽成独立扫描，在**失败路径上也跑**并写进 manifest 的 `tool_policy_violations`。顺带堵掉一个**潜伏阻塞**：trace 审计原先见到任何非 `mcp__gateway__` 工具就抛错，而 gateway 工具是延迟注册的、每轮开场必调 `ToolSearch`——这道门只在成功路径上跑，所以从没暴露，Allow 腿一旦跑通就会被它当场判死；现已把 `ToolSearch` 显式白名单化且不计入调用序列。
- **③ 残留文字：已按选定的 C 方案（预检 fail-fast，不动任何安全判据）实现。** 新增 debug 专用 **R 级**工具 `p0_probe_region_state`（判据与 `P0FocusProbeValidator.build` 共用同一实现，几何不另写一份；顺带把候选区文字/发送控件判定抽成 `probeRegionTexts`/`visibleSendControl` 三处共用），PC 侧 `scripts/lib/p0-probe-region-precheck.ps1` 经 MCP 零 token 直接问；runner 每腿开跑前调用，非空则**在派单之前**失败并回显残留文字与清框指引，预检本身不可用只警告不阻断。**预检同时覆盖「微信没停在会话页」**——判据直接复用 `P0FocusProbeValidator.build`（含 OCR 抖动重试），不 ready 时回显宏自己的逐条原因；2026-07-26 15:50 那轮真机就是停在聊天列表、在 `search_entry` 白烧 $0.32。
- **确认卡取证缺口（②）已下手，待真机判定。** 翻旧证据图实锤：人点了「允许本次」，同一次流程的 `confirmation.png` 里只有微信会话页、没有卡。已知不是缓存（每次都是新 `takeScreenshot`），也不是「卡还没画出来」（展示路径早有 frame commit fence），最可能是 **frame commit 只保证本进程提交这一帧，SurfaceFlinger 合成还差一两个 vsync**。已改：按卡的真实位置与底色 `#F2222222` 核一遍截图（只采左内边距竖带，文字/按钮都在 padding 之内），没拍到就 400ms 重拍、最多 4 次；仍拍不到照样落盘但如实标 `card_visible=false`，runner 当场红字警告并写进 manifest。**刻意不 fail-closed**——万一根因是 a11y 截图结构性看不见自家 overlay，硬拒会让整套跑测再也跑不起来。离线：debug 221 + release 133 全绿，runner 36/36。
- **2026-07-26 傍晚真机（16:51 那轮）：前两道坎已过，卡在第三道。** ①`foreground_reason:none`——前台身份问题**已解决**（`settledWindows`）；②**确认卡取证缺口已闭合**：证据图里卡完整可见（确认编号 / 目标会话 / IME 会话 / 明文预览 / 两个禁用态按钮），重拍+底色核实生效；③人点了「允许本次」之后 `press_key` 返回 `E_STALE_REF「确认后焦点输入身份已变化或无法识别」`；④审计采集其实一直是坏的（见下），此前一直藏在 ToolSearch 误杀后面。
- **③④ 根因均已定位并修（待下一轮真机验证）**：③自家确认卡是**可获焦 overlay**，抢走窗口焦点会同时造成"前台 unknown"和"IME 会话换新"两种症状——同一病根的两次发作。已给卡加 `FLAG_NOT_FOCUSABLE`（不可获焦窗口照样收触摸，按钮不受影响），`settledWindows` 留作兜底。同时按项目自己的规矩把 `validateContext` 里四处"A 或 B 或 C 变了"的合并信息拆成逐条点名 + 旧值→新值（判据一字未放宽）。④`run-as` 在 Android 11+ 读不了自己的 external files 目录（本机实测 Permission denied），而普通 `adb shell` 读得到；`exec-out` 又会把 stderr 写进目标文件冒充内容。已去掉审计两条命令的 `run-as` 并取消 `-AllowFailure`。
- **2026-07-26 17:21 那轮：P0 安全硬门首次端到端跑通。** `macro_run` ✅ → `type_text` ✅ → **确认卡弹出、真人「允许本次」、`press_key(enter)` 返回 `ok:true`（`confirmation=allowed;context=rechecked`）** ✅ → `ui_find` ✅。`FLAG_NOT_FOCUSABLE` 判断成立：前台身份与 IME 会话身份全程未被自家的卡打断，`E_BLOCKED`/`E_STALE_REF` 两条都不再出现。审计也第一次真的读到了。
- **但消息其实没发出去（新问题，与安全门无关）**：跑完截图核对——marker 原封不动躺在输入框里，会话最后一条仍是 10:32 的旧消息。`ImeBridge.enter()` 先 `performEditorAction(IME_ACTION_SEND)`，**这个调用只要 InputConnection 活着就返回 true**，微信不理会该动作时照样"成功"，于是 `KEYCODE_ENTER` 兜底路径成了死代码。已加发送后验（Enter 前后各读一次输入栏 OCR，内容没消失就 `E_VERIFY_FAIL`）——**只判定、绝不换通道重试**，因为后验失败时有可能其实已经发出去了，重试的风险是重复发送。"微信到底怎么才能发出去"是下一个待定问题（可能要允许点 发送 按钮，属新的通道决策，未擅自动手）。
- **审计采集这轮又暴露一层**：`macro_run` 写出的审计行 `channel` 为空串，而审计契约要求非空 → runner 报"必需字段为空或无效"。已把 `macro_run` 归到 `a11y`，并把兜底从 `""` 改成 `"tool"`。
- **发送通道诊断已闭环（2026-07-26 晚，零 token 真机只读）**：新增 R 级 `ime_editor_info`（读我们自己 IME 的 `EditorInfo`，只出契约字段）。实测微信聊天框 `imeOptions=0x4`（`IME_ACTION_SEND`）、单行、`no_enter_action=false`——**契约上两条路都该通，所以失败不在契约**。据此改 `ImeBridge.enter()`：**按契约选通道、只送一次**——单行框直接送物理回车键事件（单行 `TextView` 自己把回车转成 editor action，与真实键盘同路径），多行框才调 editor action；不再"先 editor action 失败再退按键"（那条链因为 `performEditorAction` 恒 true 而永远走第一条，且换通道重试等于冒重复发送的风险）。**待真机验证。**
- **2026-07-26 21:41 那轮：单行框送物理回车，微信同样不发。** `press_key` 已过安全门（确认卡→真人允许→执行），但 marker 仍在输入框、会话最后一条还是 10:32。**至此 IME 侧两条路都验完了：`performEditorAction(IME_ACTION_SEND)` 与 `KEYCODE_ENTER` 微信都不响应**，尽管该框声明 `imeOptions=IME_ACTION_SEND`、单行、未禁回车动作。合理推测是微信的发送处理与它自己的"键盘已弹起/文字模式"UI 状态机绑定（零 UI IME 下它连 发送 按钮都不渲染）。**下一步是通道决策，未擅自动手。**
- **同轮暴露我自己引入的一个缺陷（已修）**：发送后验拿"Enter 之前那次 OCR 读回的字符串"当基线做 contains 比较，而 OCR 每次噪声不同（同一屏读出 `") POALLOW-…F20 ) POALLOW-…F2C"` 与 `"POALLOW-…F2C"`），任何抖动都会被判成"内容已消失"→ **`sent_verified:true` 谎报发送成功**。已改为以**已提交的确切文本**（输入证据的 preview，长度未超 64 时即原文）为基线。这轮是 runner 侧独立后验（ui_find 命中落在输入栏而非消息区）把它兜住的——**两层后验各自独立，价值就在这里**。
- **发送不出去的真因大概率是微信自己的开关，不是通道（2026-07-26 收尾发现）**：我们**自己的** [apps/wechat.md](docs/knowledge/apps/wechat.md) 2026-07-22 那条早就写着"微信「使用回车键发送消息」开启时 `ACTION_IME_ENTER` 触发发送"。该开关默认关闭时，微信压根没把回车接到发送上——`imeOptions` 声明 `IME_ACTION_SEND` 只决定键盘画什么键，不决定 App 怎么处理。这与"零 UI 键盘下不渲染发送按钮"完全自洽。**排查时只读了 android/common.md 没读 app 册，为此多烧两轮真机**；已把该前置写进 runbook §3.0 与 app 册。**下一轮开开关再跑即可判定。**
- **临时设备状态（需还原）**：当前输入法已被切成「执行网关」，原值 `com.sohu.inputmethod.sogou.vivo/.SogouIME`；跑完记得切回。
- **遗留/障碍**：真实 Allow/Stale 尚未触发确认卡（根因见上，已从"概率性因素"升级为架构级阻断）；`Start-P0TargetApp` 跳过 `am start` 的修复有副作用——服务重启后无人触发窗口状态事件，网关前台身份为 unknown 而拒绝执行，当前靠"人工回桌面再让脚本拉起微信"绕过，需正式修（让 tracker 能冷启动自举）；Deny 确认卡截图沿用既有证据缺口；S5 RemoteInput、S2 Shizuku 重启存活、`dispatch.ps1 -Confirm` 收口、`share_file` activity 级 verify 待补；Codex 订阅额度已耗尽（2026-07-30 恢复，现已非构建阻塞项）。
