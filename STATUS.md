# 项目状态

> 每次会话收尾更新。保持 ≤ 20 行。**这里只放"影响下一步怎么做"的事**；
> 已闭环批次的详细复盘移出到 [docs/status-archive.md](docs/status-archive.md)（只写不读），
> 教训的正式落点是 [knowledge/](docs/knowledge/README.md) 与 [backlog 复核清单](docs/backlog.md)。

- **已通过并合入 main 的：批次 1**（干掉每跑一腿的人工前后置，`337113c`）· **批次 2**（通知栏审批）· **批次 3**（Deny 带外验证），后两批 `f08cda2`。批次 1 三腿一次连跑、`teardown.verdict` 全 `clean`、人只点 3 次卡。详细复盘见归档。
- **批次 2 判据 1 收窄为「屏幕亮着但人没盯着」**：锁屏审批与 D1 结构性冲突（`requireKnownForeground` 跑在 `policy.assess` 之前，锁屏时**卡与通知都不会出现**），用户 08-02 拍板"先收窄、再做语义意图"。**锁屏两条如实归档为从未验过、不当成通过。**
- **工序按"是否消耗人的精力"分三道并行**（[backlog](docs/backlog.md)）：A 独立闭环 / B 你一个决定 / C 你在真机旁。改动堆成**验收批次**、钉 commit SHA，把人的参与从"每个改动一次跑测"降到"一批一次"。队列写权在主会话，工序会话经 `git show main:` 读，干完主动 `send_message` 叫醒主会话。
- **语义意图审批 + 批准后重建证据：离线全部做完，在分支 `claude/serene-faraday-42d1fb` 上，main 行为一个字没变。** 生产装配已完成（钉 `67ab582`），开关随之打开；`§9.6` 会话标题读取器**已下沉到 `src/main`**，宏改调同一份，`ConversationSurfacePolicyTest` 在 `testReleaseUnitTest` 里跑得通即是"release 里也在"的机械证据。JVM 442 测试。
- **意图有效期用户 08-03 拍板 5 分钟**（语义："批准后最多隔多久回到微信"）：`foregroundWaitBudget`→300s、`intentTtl`→360s、`decisionTimeout` 保持 90s，关系写成构造断言（`intentTtl ≥ foregroundWaitBudget`；等前台超过证据 TTL 则**必须装配重建通道**——断言挂在能看见通道装没装的地方，不挂可写错的布尔）。**放宽有效期不放宽内容完整性**：执行前重读并与卡上摘要比对。
- **两处证据都要重建，两次都是离线挖出来的"上真机必然白跑"**：①输入证据按焦点身份取，而 IME 会话 id 每次 `onStartInput` 自增哈希必变；②`PreparedTargetEvidence` 同为 120s 硬要求。**两次的失败形态都与今天一模一样。** 三态 `Rebuilt`/`Mismatch`/`Unverified`，基线一律是卡上那份，**先验会话再验内容**。
- **比对方式两档**：a11y 严格 sha256；**OCR 归一包含**（`sha256` 逐位相等在 OCR-only 链上物理不可满足，且失败方向是**诬告用户改过内容**）。`contains` 弱在"发得比批准的多"→ 加长度守卫（容差 4）；**漏识导致的不匹配判 `Unverified` 而不是 `Mismatch`**——两者在 OCR-only 链上物理不可分，"读不准不敢放行" ≠ "确认你改过"。
- **批次 4 跑到第四轮（08-08~09），三腿一直全过，传输层天花板三次确认没了**（`press_key` 阻塞 83.9/99.8/86.5s 都拿回正经错误码）。三处离线修复真机全验到：`last_package` 不再吞后两段 · `sse_heartbeat` 每腿都落（Allow `beats=0` 与 Reentry `beats=1`，"自洽"与"没接上"分得开）· Allow 腿 `title_read` 如实记 `reported:false`。台账 8 行在 main。
- **第四跑：分因一跑锁死方向，「读不回来」解开了**（`trail=no_snapshot+no_snapshot+resolved`，2.2s 后第 3 次读到）。**是时机，但既不是识别质量也不是融合闸门——是快照本身**：微信刚被拉回前台那一瞬间连屏幕都拿不到（`fg=-1`）。有界重试正是解药。三跑之前那句 `channel=ocr` 把所有人指向识别质量，"渲染时机"这个假设只猜对一半。
- **但撞上新墙，而且它真的诬告了用户**：`E_STALE_REF`「目标会话与已批准的不符：文件传输助手 → **7.70KB/s**」——那是状态栏的实时网速，**用户全程没换过会话**。根因：**a11y 跨窗口，状态栏是它自己的窗口**，而标题带上沿是 2%×2800=56、状态栏文字中心**实测 65~70px**，**落在带内**（我最初写的 55 是错的：55 在带外，那样它根本不该成为候选，**解释与它要解释的现象自相矛盾**——已进复核清单）。修法两道、能力边界不同故缺一不可：**窗口归属**（有节点的元素必须来自前台应用窗口，OCR 元素没窗口可归属）+ **状态栏下沿**（`system_top_inset` 走真实窗口几何，不按比例猜——按比例那次偏了 130px）。两道各有独立正对照。
- **标题这一处从此永不产出 `Mismatch`**（spec §9.10）：判别式是"我能不能证明这是读错了"，而标题通道**结构上给不出正证据**（带内多候选、混进别的窗口、OCR 漏识多识）。**两态都不放行，变的只是错误码与那句话**（`E_STALE_REF`「你换了会话」→ `E_VERIFY_FAIL`「我没敢认」）——代价既然只在那句话上，就没有理由挑指责用户的那一句。用例钉的是"遍历五种标签×两条通道都不产出它"，不是单个例子。**推断变观测**：`uiautomator dump` 看不见状态栏（只抓前台应用窗口），**跨窗口的东西只有网关看得见**，故候选清单由网关逐个列出（文本+几何+来源+窗口归属+为什么没选中，进错误信息）；纯数字 `sysrej` 进 note 与 manifest，那一帧上是 6。
- **runbook 那句「`E_VERIFY_FAIL` + 读不回来 → 重跑即可」已更正**：第三跑三次逐字相同，按那句话跑就是拿真机时间撞一堵不会动的墙。现在要求先看 `trail` 再决定要不要重跑。**批次 4 现场三条防误判仍有效**（Stale 腿几十秒终态是对的、挡它的理由必须是"等前台恢复超时"、OCR 抖动的 `Unverified` 是正确的 fail-closed）；`-Provision` 装 debug APK，通过区分不了"功能成立"与"只在 debug 里成立"。
- **跑前物理前置（都付过学费）**：微信停在「文件传输助手」· 输入框空 · 「回车发送」开着 · `zen_mode=0` · **输入栏上方无系统浮层**（联通流量提示那次把 teardown 判成 dirty）· **切 App 要真的切过去、界面显示出来再点通知**（上轮首跑没真切走，网关仍读到微信而以 `E_CHANNEL_DOWN` 终止）· **上一轮中途断过线的话，开跑前先确认输入法已还原**（断开同时打死拉回与全部收尾，`restore_ime` 在收尾里；**这条与断开的原因无关**，不写成"检查线材"——那种检查项永远验不出问题，还会把真因盖住）· **`zen_mode` 现场读一次再开跑**（vivo 定时睡眠会自己打开，第四跑开跑前实测就是 1；深夜跑测反复撞）· **跑真机时别同时跑离线闸门**（310MB 可用内存下套件退化成顺序单分片，成片超时与代码无关却最像"新判据不稳"）。
- **判据纪律（本轮反复付学费，已进 [backlog 复核清单](docs/backlog.md)）**：断言源码文本 = 未覆盖 · 断言"个数>0"会替坏掉的注入打掩护 · **对照实验本身也需要一次对照**（短路没写进去 → 全绿 → 会读成"判据不灵"，方向正好反）· **判据与物理通道相不相称，要等真的去接才暴露** · **转述别人的现场结论时先问"这是观测还是推断"**（"USB 掉线不是操作问题"是从"半小时掉两次"推出来的、真因是人手动拔线，我据此差点把一条永远验不出问题的检查项写进 runbook；两天内第二次）· **往共享串里加字段会把原本在串尾的字段变成串中的**——`last=(?<last>\S*)` 贪吃掉了后面两段，而离线撞不到是因为**假件把字段顺序摆得比真件干净**（做过对照：保持贪吃正则不变、只把 fixture 顺序改回去，106 条立刻全绿） · 判据要挂在能被机械验证的东西上，不挂需要有人记得填对的字段。
- **遗留/障碍**：**开关打开后非宏路径的危险 `ui_action` 一律 `E_STALE_REF`**（只有 P0 准备宏记录目标会话证据；fail-closed 且今天无人走该路径，但**这处收窄在本批验收里看不出来**，接任务 4/2 端到端前必须解决，已进 [backlog §6](docs/backlog.md)）；C 道新 worktree 首跑要 `local.properties`（gitignored）+ 先构建 APK，**已由预热踩掉**；跑 runner 前不要手动切 IME（会把"原输入法"记成网关）；审计目录迁 filesDir 未做；S5 RemoteInput、S2 Shizuku 重启存活、`share_file` activity 级 verify 待补；最长函数是 `ConfirmOverlay.ask`（307 行）、`ToolRegistry.callInternal`（180）第二——**"callInternal 179 全仓最长"是第三次量错后被 [harness.md](docs/knowledge/brain/harness.md) 纠正的旧说法，我压缩 STATUS 时又把它照抄了一遍**。
