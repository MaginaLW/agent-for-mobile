# 项目状态

> 每次会话收尾更新。保持 ≤ 20 行。

- **当前阶段：P0 Allow 腿真机验收 ✅ 通过（2026-07-31 20:32，V2352A/Android16，run_id `20260731T203240-f37d3c3eac3d`）。** manifest `status=passed`、`cleanup.ok=true`、`confirmation=allowed`、`safety_code=OK`、`dangerous_calls=1`、`input_evidence_matched=true`、`card_visible=true`、`send_postcondition=single_match`。**消息真的发出去了，用户肉眼独立确认。** 注脚：`send_verification=unverified`——微信 OCR-only 链下输入栏清空后读不到任何文字，网关侧无法自证，本腿判通过依据的是 runner 侧 `ui_find` 在消息区命中 marker 这条独立正证据（日志如实打印）。三态后验的 `unverified` 本来就该由调用方正证据兜底。
- **卡了五轮的真因：`press_key` 用未过滤的焦点节点走 a11y 通道，把 IME 通道短路了。** `viaNode` 直接对 `findFocus(FOCUS_INPUT)` 调 `ACTION_IME_ENTER`；微信会话页那个残留节点既不 focused 也不 editable，却**接受该动作并返回 true**（假成功），于是 `viaNode || ImeBridge.enter()` 当场短路。**铁证**：同一次调用序列里 `type_text` 报 `ime_commit_ocr`（无节点通道）而 `press_key` 报 `a11y_ime_enter`——两个工具对"有没有可用节点"的判断互相矛盾。这是《残留焦点节点连累三处》的**第四处**，而那条教训原文就是"要把所有取用该节点的路径一起改"。已统一到 `FocusedInputSnapshot.nodeUsableForAction`（只认 `IdentitySource.A11Y`）并配离线用例。修好后通道变为 `editor_action:send`，消息即发出。
- **方法论（本轮最值钱的一条，已进 knowledge）：修不动的时候先加可观测性，别加假设。** 第 2 轮改完 EnterStrategy、结果照旧，而返回里没有通道信息，**连"修的地方有没有被执行到"都验证不了**；第 3 轮没有再加假设，只给危险动作加了 `enter_channel`（走的哪条路 + 输入框契约，成功失败都带），当轮定位真因。「回车开关」那条假设也在第 1 轮被证伪（开关已开仍发不出去）。
- **危险动作的返回值必须由后验决定**：`performEditorAction` 只要连接活着就返回 true，微信不理会时照样"成功"。已加发送后验（**只判定、绝不换通道重试**——后验失败时可能其实已发出，重试等于冒重复发送）。后验自身还踩过一次假阳性：基线用了 Enter 前那次 OCR 读回的噪声串，已改用已提交文本原文。那次是 runner 侧独立后验兜住的。
- **后验已改三态并补齐判据（2026-07-26 晚，仅离线验证）**：布尔版把"判不了"当"没发出去"，最要命的是**>64 字的消息即使真的发出去也必然报失败**（基线取自 64 字上限的 `preview`，超长被截断即取不到基线）；测试 marker 18 字恰好绕过，离线真机都测不到。现为 `sent/not_sent/unverified`，`unverified` 按 ok 返回并要求只读复核；判据 a11y 优先、OCR 降级；固定 600ms 改轮询。runner 侧新增"Allow 腿必须 `sent_verified=true`"断言（细节见 [android/common.md](docs/knowledge/android/common.md)）。
- **harness 三处摩擦已修**（离线 dispatch 17/17、runner 36/36）：①派单锁按"能否独占打开"判活，崩溃残锁自动清；②`--allowedTools` 只是免确认名单——执行器真在本机跑起了 `Bash`，已补 `--disallowedTools`，越权扫描改为**失败路径上也跑**并写进 manifest；③开跑前零 token 预检（R 级 `p0_probe_region_state`，判据与宏共用同一实现），残留文字与"没停在会话页"都在派单之前拦下。顺带堵掉潜伏阻塞：`ToolSearch` 此前会被 trace 审计当成越权，Allow 腿一跑通就会被它当场判死。
- **顺带修正的 Enter 通道选择（离线，第 2 轮引入）**：`ImeBridge.enter()` 原按「是不是多行」二选一且接反了——多行走 `performEditorAction`、单行发裸 KeyEvent，而微信聊天框是 `IME_ACTION_SEND` + **单行** + `no_enter_action=false`，恰恰绕开了它自己宣告的 SEND 动作。安卓的约定是看 `NO_ENTER_ACTION`+actionCode，不看 multiLine。已改正（`EnterStrategyTest` 6 条用例，微信实测契约作回归数据），并把裸按键改成 AOSP 标准软键盘事件（`VIRTUAL_KEYBOARD` + `FLAG_SOFT_KEYBOARD|FLAG_KEEP_TOUCH_MODE`）。**注意：这条修复本身没解决问题**（它被上面的节点短路挡着没执行到），但修好短路后正是它让 `editor_action:send` 生效。
- **harness 判据五处修复（都由这九轮真机逼出来）**：①执行器调 `ReportFindings` 被越权扫描判死，**且发生在真人已允许、危险动作已执行之后**——拒绝名单按类补严，同轮发现 **`PowerShell` 一直没禁**（只禁了 `Bash`，而本机是 Windows）；②marker 归一化未做 O→0，把一次真正成功的发送判成"证据不匹配"（网关 `OcrEngine.norm` 早就这么做，runner 比它还严）；③"marker 在消息区"判据依赖 a11y 焦点几何，**在微信上结构性不可满足**，改为焦点几何结构性缺失时用设备自报的输入栏候选区划线（焦点几何存在但变化仍走严格判据，不放宽）；④零 token 预检在 `-Provision` 后必失效（服务刚重启），加就绪重试；⑤终态报告不认 markdown 粗体——**而 dispatch 同一处会把 `**结果：失败**` 落进兜底分支记成 success**，台账层面最危险的错分，模式已收进 `dispatch-ledger.ps1` 由两处共用。
- **下一步**：①跑 Stale 与 Deny 两腿（Deny 是唯一直接证明"不批准就绝不执行"的一腿，今天刚接进 runner、从未上过真机）；②补 Deny 腿的带外验证（见下）（IME 显示极简输入视图逼出发送按钮 / 换一个对回车有反应的目标先验证后验链 / 等 Shizuku 注入真实按键）；③冷启动自举（服务重启后 `foreground_reason:identity_unset`）。随后 D3、Shizuku、IME 自动切换及任务 4/2 端到端链路。
- **仓库整改（2026-07-26~27，8 次提交，均未推送；离线全绿：dispatch 21/21、runner 44/44）**：A 仓库卫生 + **危险动作提示词从 1204 行 PowerShell 收进 `scripts/tasks/*.tmpl.md`**（黄金回归逐字钉住）· B 一键校验 `scripts/check.ps1` · C 发送后验三态 · **跑测环境真因**（假 adb 里 `find` 被 Git Bash 的 Unix find 顶替、递归扫 C 盘到 30s 超时；此前误诊成机器负载与 stdin 各一轮）· D 凭据与审计加固 · E **Deny 腿接进 runner**（4 类失败各有用例）· F 台账 `fail_reason` 列。**剩余 F**：审计目录迁 filesDir（**需真机验证 `run-as` 能读该路径，不属"纯离线"可完成项**）、套件提速、`ToolRegistry.callInternal`（179 行，全仓最长）。
- **子代理复查（2026-07-27）修掉 9 处，其中两处是本轮核心改动自身的缺口**：①`fromOcrReadback` 把"OCR 一个字没读到"判成已发送——**换了触发条件的谎报成功**，且被我自己的用例固化成预期行为；现在只有"读到非空文本且不含基线"才判 SENT。②因此 Allow 腿"必须 `sent_verified=true`"的断言写反了：微信 OCR-only 链下发送成功后输入栏本就是空的，强求自证会让 P0 **永远过不了**；改为只禁矛盾（`not_sent`），正证据仍由 runner 侧 `ui_find` 承担。③Deny 提示词与严格签名自相矛盾（`E_BLOCKED` 在该腿是预期结果，却会触发取证例外多调一次）。④后验预算 2s→6s。⑤a11y 腿补身份校验。⑥`preferredBounds` 补 ≥32 守卫。⑦TokenStore 落盘失败不再静默缓存。⑧`Stop-DevEnvGradleDaemon` 全机杀改显式 opt-in。⑨多处夸大的注释/manifest 字段改成如实措辞。
- **执行器工具面两次咬人，已按类补严**：`--allowedTools` 只是免确认名单，真正限制的是 `--disallowedTools`，而它是手写枚举。2026-07-26 `ToolSearch` 被越权审计误杀；2026-07-31 执行器调 `ReportFindings`，**在真人已允许、危险动作已执行之后**把整腿判死。同轮发现 **`PowerShell` 一直没禁**（只禁了 `Bash`，而本机是 Windows，仓库里放着 gateway token）。现按类分组并加离线断言（两个 shell 必须成对）。详见 [brain/harness.md](docs/knowledge/brain/harness.md)。
- **设备状态**：2026-07-31 那轮 runner 已自动重装 APK 并恢复原输入法；用户手动删掉了没发出去的 marker 文本。跑 runner 前**不要手动改设备状态**——我这轮手动切过 IME，导致 runner 把"原输入法"记成了网关。
- **OCR 融合判定抽成纯函数 [OcrFusionPolicy.kt](app/gateway/src/main/java/dev/magina/gateway/a11y/OcrFusionPolicy.kt)（2026-07-27，15 条离线用例）**：挂载/去重/独立成元素这套规则原先埋在 `snapshot()` 的 55 行循环里，改一处要烧一轮真机；现在可离线钉死，含 2026-07-19 那条真机教训（带文本的大悬浮节点罩住页面文字时不得按重复吞掉）。**两次更正（同一个度量错误犯了两遍）**：此前写的"`snapshot()` 317 行"与"`gatedOnly` 232 行"都是错的——两次都把"到下一个 `fun` 的距离"当成函数长度，而中间夹着 `    internal fun`（grep 漏匹配）和 `val tools = listOf(...)`（声明式属性，不是函数）。`gatedOnly` 实为 3 行，`snapshot` 改动前 138 行。**用括号匹配重测**：全仓最长函数是 `ToolRegistry.callInternal`（179 行），其次 `snapshot`（130）。**没有任何 200 行以上的函数**，最初审查里"巨型函数"这条被我夸大了两次。
- **Deny 腿的证明力有边界，用它的结论时要知道**：四条判据（`E_BLOCKED`、审计一致、零续调、`sent_verified` 非 true）**全部来自被测组件自己的报告**，runner 没有独立观察屏幕。它能证明"网关声称拒绝了"，不能证明"消息确实没发出去"。补法是腿末经 runner 自己的 adb 通道做一次带外截屏/OCR 比对（不经执行器、不进 trace）；在此之前 manifest 如实记 `gateway_reported_blocked_no_independent_check`。
- **遗留/障碍**：Stale 腿与 Deny 腿尚未在新链路下复跑；`Start-P0TargetApp` 跳过 `am start` 的副作用（服务重启后前台身份 unknown）仍靠人工绕过，正式修法是 tracker 冷启动自举；S5 RemoteInput、S2 Shizuku 重启存活、`dispatch.ps1 -Confirm` 收口、`share_file` activity 级 verify 待补；Codex 订阅额度 2026-07-30 恢复（非阻塞）。
