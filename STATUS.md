# 项目状态

> 每次会话收尾更新。保持 ≤ 20 行。**这里只放"影响下一步怎么做"的事**；
> 已闭环批次的详细复盘移出到 [docs/status-archive.md](docs/status-archive.md)（只写不读），
> 教训的正式落点是 [knowledge/](docs/knowledge/README.md) 与 [backlog 复核清单](docs/backlog.md)。

- **已通过并合入 main 的：批次 1**（干掉每跑一腿的人工前后置，`337113c`）· **批次 2**（通知栏审批）· **批次 3**（Deny 带外验证），后两批 `f08cda2`。批次 1 三腿一次连跑、`teardown.verdict` 全 `clean`、人只点 3 次卡。详细复盘见归档。
- **批次 2 判据 1 收窄为「屏幕亮着但人没盯着」**：锁屏审批与 D1 结构性冲突（`requireKnownForeground` 跑在 `policy.assess` 之前，锁屏时**卡与通知都不会出现**），用户 08-02 拍板"先收窄、再做语义意图"。**锁屏两条如实归档为从未验过、不当成通过。**
- **工序按"是否消耗人的精力"分三道并行**（[backlog](docs/backlog.md)）：A 独立闭环 / B 你一个决定 / C 你在真机旁。改动堆成**验收批次**、钉 commit SHA，把人的参与从"每个改动一次跑测"降到"一批一次"。队列写权在主会话，工序会话经 `git show main:` 读，干完主动 `send_message` 叫醒主会话。
- **批次 4 最新离线候选为 `67ef56cc8289b34d09843701d7b83986a206ad0e`，分支 `codex/batch4-precheck-unify`；main 行为仍未合入。** 它包含输入 OCR clique 折叠、canonical partial parser、Codex 0.149 契约和 precheck/生产标题统一；Gateway Debug 524/524、Release 418/418、runner 153/153、assembleDebug/凭据扫描与独审 0/0/0 全绿。
- **意图有效期用户 08-03 拍板 5 分钟**（语义："批准后最多隔多久回到微信"）：`decisionTimeout` 从审批请求建立起 90s；真人允许胜出时才一次性写不可刷新的 `approvedAtMs`，从此 `foregroundWaitBudget`→300s、`intentTtl`→360s。关系写成构造断言（`intentTtl ≥ foregroundWaitBudget`；等前台超过证据 TTL 则**必须装配重建通道**——断言挂在能看见通道装没装的地方，不挂可写错的布尔）。**放宽有效期不放宽内容完整性**：执行前重读并与卡上摘要比对。
- **两处证据都要重建，两次都是离线挖出来的"上真机必然白跑"**：①输入证据按焦点身份取，而 IME 会话 id 每次 `onStartInput` 自增哈希必变；②`PreparedTargetEvidence` 同为 120s 硬要求。**两次的失败形态都与今天一模一样。** 三态 `Rebuilt`/`Mismatch`/`Unverified`，基线一律是卡上那份，**先验会话再验内容**。
- **比对方式两档**：a11y 严格 sha256；**OCR 归一包含**（`sha256` 逐位相等在 OCR-only 链上物理不可满足，且失败方向是**诬告用户改过内容**）。`contains` 弱在"发得比批准的多"→ 加长度守卫（容差 4）；**漏识导致的不匹配判 `Unverified` 而不是 `Mismatch`**——两者在 OCR-only 链上物理不可分，"读不准不敢放行" ≠ "确认你改过"。
- **四条旧 C 均冻结且不复用**：占位通道 run `20260809T203420-6cf147532b9f` 只算基础设施失败；`3ed077d` run `20260811T202517-0e176d3f08b9` 因标题误选正确 fail-closed；`d36e3d2` run `20260811T215340-d3eb4c2bdeaf` 消息已发但被 runner canonical 误判；`f0a7673` task `019ff195-0fde-7eb2-ac1a-88ee11cc1a2d` / run `20260813T201212-3e9ae5507700` 在 Allow 因 OCR 同一 marker 两份拼接、最终长度多 23 字而正确 fail-closed。最新一轮 teardown clean、cleanup true，ledger 已在 main 恰好一行，13 文件持久证据 12/12 哈希通过。
- **手机 C 道已暂停；PA2553 / Android 16 / 日常横屏为当前真机默认。T-L1 C1a 仍只建立 trusted origin/read-only，diagnostic/T-L1/P0/execution 未通过。C1b 第七次 one-shot `a661f365322bbb8ef3aa83f7a84b9bcb23a51f6e` 已失败并冻结，不得重跑。** r13 read-only preflight 外部 exit 0、三阶段 ADB/5037=`0/0`；授权 smoke 随后因 Git file count 从冻结 `9576` 漂移到 `9577` 在 Gradle 前 fail-closed，退出后独立重算又确认 full-tree catalog 从 `4c5e…7458` 漂移到 `deeaa…34ee`。launcher 还因 `return $bytes` 被展开成 `Object[]`、在 `Buffer.BlockCopy` 抛错而遮蔽 helper primary；Gradle/build/设备均未执行，四件失败证据已只读冻结。**数组返回 P1 已修：** repo-external `launcher-template-r11.ps1` 只插入 unary comma，145140 B / `f31b944f…e817`，r10 未改；`3ef4714` 已补 ArrayLiteralAst/mutation、0/1/N、passed/failed、SHA/BlockCopy/CRLF 与 failure-first 常驻回归，固定 7.6.4 专项和 29-case 聚合门通过。Git A r1/r2 均已按失败分支停止；**主机侧已于 09-02 用一次移出动作恢复并四门闭合（见下条），A→E 阶梯作废、新 pair 未消费**。主机已定版，**下一步是 repo 定版**：固定最终 clean SHA、生成并静态复核 r11 pair 与 r14 preflight。静态复核通过后只运行一次 r14 preflight；该次运行必须在四次 repo 只读 Git 前后各用同一 BCL/Win32 count/catalog/identity/hardlink 规则验证全树，任一终态漂移即拒绝；只有该次 r14 闭合后，才另取该 SHA 的 build-only one-shot 授权。install/设备/C1b 采集及 navigation/conversation/target/regions/layout/微信/editor/action/P0/execution 仍未放行。
- **Git 信任根已恢复并四门闭合（09-02），五道重装阶梯（A→E）作废且一次授权都没消费。** 移出单个文件后独立复算：file count `9576`、catalog `4c5e585b…7458`、identity `9489`、hardlink group `85` 全 PASS，hardlink 全闭合在 root 内；`git version` 仍 `2.55.0.windows.3`、三个 pinned key file SHA 一致，移出未影响任何 Git 功能。**根因：漂移是「多了一个文件」，不是「改了一个文件」。** 只读全树复算证明唯一新增项是 `usr/bin/busybox.exe`（864256 B / `334843de…6e92`，busybox-w32 1.38.0，创建于 `2026-08-29T16:55:36Z`，而其余 9576 个文件全部创建于安装日 `2026-08-18`）；**排除它之后 catalog 精确回到冻结 `4c5e…7458`，即其余 9576 个逐字节未变、无任何 Git 二进制被篡改**。复算规则的对照是它独立重现了 smoke 记录的漂移值 `deeaa…34ee`。**该文件不在 Inno 卸载日志内（对照：`git.exe`=14/`bash.exe`=11/`busybox`=0），所以卸载+重装后它仍在、门仍 fail-closed——两次已消费的 A 授权走在结构上无法闭合的路径上。** **未确认项如实标注**：字节是否来自正版 PortableGit 2.55.0.3 未取证（mtime 与打包时间戳一致只是推断，本机无 `7z`/`innoextract`）；写入者与提权来源未知。冻结的 leaf r2 `a78c99e4…cd66` / parent r3 `fd7d9b6d…2a25` 未授权、未执行、原样保留，仅在改走重装路线时才需要。详见 [漂移定位记录](docs/runs/2026-09-02-T-L1-C1b-Git信任根漂移定位.md)。
- **批次 4 现场三条防误判**（验收单已写）：**Stale 腿几十秒终态是对的**，且**挡住它的理由是"等前台恢复超时"而不是"包变了"**；**OCR 抖动导致的 `Unverified` 是正确的 fail-closed，不是功能不稳定**；`-Provision` 装的是 **debug APK**，所以**通过不能区分"功能成立"与"功能只在 debug 里成立"**。
- **平板适配原则（用户 08-25 拍板）**：项目适配设备原生能力，不靠关闭设备功能换绿；用户按既定现场保持 vivo 横屏“应用多窗”，成功 C1a 的机械证据是 T0 `multi_landscape` 与同 App 双窗口，runner 未读取开关值，也未修改 settings、启动目标 App 或截图。关闭功能最多只能做显式对照实验，不能成为产品前置或验收条件；本轮只证明可在该双窗形态下完成可信只读诊断，布局适配仍 blocked；手机 `h>w`/比例/标题底栏门仍不放宽。
- **判据纪律（本轮反复付学费，已进 [backlog 复核清单](docs/backlog.md)）**：断言源码文本 = 未覆盖 · fixture 复刻 consumer 而非真实 producer 会让两边同错仍全绿 · **对照实验本身也需要一次对照** · **判据与物理通道相不相称，要等真的去接才暴露** · 判据要挂在能被机械验证的东西上，不挂需要有人记得填对的字段。
- **遗留/障碍**：**开关打开后非宏路径的危险 `ui_action` 一律 `E_STALE_REF`**（只有 P0 准备宏记录目标会话证据；fail-closed 且今天无人走该路径，但**这处收窄在本批验收里看不出来**，接任务 4/2 端到端前必须解决，已进 [backlog §6](docs/backlog.md)）；C 道新 worktree 首跑要 `local.properties`（gitignored）+ 先构建 APK，**已由预热踩掉**；跑 runner 前不要手动切 IME（会把"原输入法"记成网关）；审计目录迁 filesDir 未做；S5 RemoteInput、S2 Shizuku 重启存活、`share_file` activity 级 verify 待补；最长函数是 `ConfirmOverlay.ask`（307 行）、`ToolRegistry.callInternal`（180）第二——**"callInternal 179 全仓最长"是第三次量错后被 [harness.md](docs/knowledge/brain/harness.md) 纠正的旧说法，我压缩 STATUS 时又把它照抄了一遍**。
