# 2026-08-23 · 批次 4 C 道安装确认超时

- 候选：`67ef56cc8289b34d09843701d7b83986a206ad0e`
- run：`20260823T095948-028b841bc24d`
- 前置：唯一 `:gateway:assembleDebug` 成功；设备唯一且在线、屏幕亮、`zen_mode=0`、微信前台、
  固定 HEAD 与 clean worktree 均机械通过。
- 结果：Provision 发起覆盖安装后，设备 120 秒始终停在 vivo
  `com.android.packageinstaller/.PackageInterceptActivity`；包 `lastUpdateTime` 未变化，runner 以
  `安装 debug APK 超时。` 在 setup 冻结。
- 证据：`legs=[]`、`cleanup.ok=true`，未生成 slug/dispatch，故 ledger 无行；持久 manifest SHA-256
  为 `4DC81FEE8F115405DA6B20802C7871BE62A90589B3613779E972787AE9D24801`。

该 run/worktree 不得重跑或复用，批次 4 仍为 0/4。相同安装确认未完成已在 run 2、run 6、run 8
出现三次，不再用新 C 反复等待；只有用户能现场确认系统安装按钮可见并在 runner 窗口内完成点击时，
才允许固定同一候选 SHA 建下一条 clean C。
