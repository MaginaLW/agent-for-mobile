# T-L1 C1a 修复后真机只读取证成功

- 日期：2026-08-26
- fixed SHA：`4b96f89a6622eb8b5fe04bd249571c7d77936b25`
- 分支：`codex/tablet-tl1-c1a`
- run：`tl1-c1a-20260826t125127z-354a7b4b0ed5`
- runner：exit 0
- 设备：vivo PA2553 / DPD2437 / Android 16 / API 36 / 日常横屏
- 结论：**C1a trusted origin/read-only 取证成功；diagnostic blocked，T-L1 未通过**

本记录只冻结 C1a 来源绑定、只读边界和真实诊断结果。它不把 diagnostic blocked 写成布局通过，不授予
runtime/P0/action/execution，也不覆盖此前失败 run
[`tl1-c1a-20260826t114535z-63667b68ce4f`](2026-08-26-T-L1-C1a只读取证失败.md)。

## 唯一执行与采集

- c1/c2 各调用一次，capture token 为 `c1`/`c2`，capture ID 为 `capture-c1`/`capture-c2`；
- 两帧 capture timestamp delta：2023.223 ms；
- runner host wait：905 ms；
- runner 总 span：3140 ms；
- `recapture_count=0`，没有补拍或自动重采；
- success sidecar 已发布，failure/tmp 文件不存在；evidence root 有且仅有下节五个标准文件；
- cleanup status 为 `not_required`：result 已成功消费，按协议无需 abort；不能把它改写成 cleanup passed。

## 五份冻结证据

evidence root：

```text
docs/runs/evidence/tl1-c1a-20260826t125127z-354a7b4b0ed5
```

| 文件 | SHA-256 |
|---|---|
| `tablet-profile.json` | `6f5b1539d3d09bf77e26dc2ba5d700d11857c3edac84eef33fee03df4a81c316` |
| `tablet-layout-c1a/upstream-t0-v5.json` | `6f5b1539d3d09bf77e26dc2ba5d700d11857c3edac84eef33fee03df4a81c316` |
| `tablet-layout-c1a/tablet-layout-observation-v2.json` | `a7be22f5a055045ab455c7a734fbe597062ea9efe20d6cd10bb441719a10a549` |
| `tablet-layout-c1a/tablet-layout-observation-validation-v2.json` | `ad3ced0159f6e53be6bbb8859bf64ce245c5b74e1bc4a14ac30a8e29e157985d` |
| `tablet-layout-c1a/tablet-layout-c1a-sidecar-v1.json` | `e43d05fed0680c2fd22ab29c54f0cdccc19443652a2f4038f6a460038073b606` |

`tablet-profile.json` 与 `upstream-t0-v5.json` 是同一份原始 T0 bytes，因此文件长度与 hash 完全相同。
本目录不含 `tablet-layout-c1a-failure.json` 或临时文件。

## T0 CRLF 修复的真机证明

两份 T0 文件均为 23,865 bytes，包含 747 个 CRLF，且不存在 bare LF 或 bare CR。sidecar 记录
`original_bytes_forwarded=true`，profile/upstream SHA-256 同为
`6f5b1539d3d09bf77e26dc2ba5d700d11857c3edac84eef33fee03df4a81c316`。

这证明 Windows 宿主改用 `adb exec-in content write` binary stdin 并传 raw canonical URI 后，T0 producer
的原始 CRLF bytes 在真机链路上保持不变；此前普通 `adb shell` stdin 的 CRLF→LF 失败没有复现。

## APK、来源绑定与只读边界

fresh APK 的 local before/after 与 installed base APK pre/post SHA-256 全部一致：

```text
0f2e5922e5f4c12b03b74fe06b7e0e40aa870ec376eca2cf06a4984ac2e4b288
```

success sidecar 冻结以下结论：

- expected commit 精确为 `4b96f89a6622eb8b5fe04bd249571c7d77936b25`；
- `c1a_origin_binding_verified=true`；
- `c1a_probe_entrypoint_read_only=true`；
- observation schema valid；
- `cleanup_status=not_required`；
- `settings_mutation_used=false`；
- `target_app_started=false`；
- `screen_capture_used=false`；
- `mcp_used=false`、`dispatch_used=false`。

因此本 run 的 origin、read-only 与 cleanup 边界成立，但它们只说明证据来源可信、入口只读，不说明微信
window/pane 布局满足 T-L1。

## 真实横屏双窗形态

两帧显示均为横屏 2800×1968，并稳定枚举两个 `com.tencent.mm` application window：

```text
[0,0,985,1968]
[989,0,2800,1968]
```

取证现场由用户保持 vivo“应用多窗”日常开启；runner 没有读取该系统开关值，也没有修改 settings、启动目标
App 或截图。机械证据是 T0 `multi_landscape` 与上述两个稳定 a11y application window，只证明 C1a 能在该
原生双窗形态下完成可信只读诊断，不证明布局已经适配；项目没有通过关闭应用多窗换绿。

C1a 两帧的当前 display 证据是 known display 0、横屏 2800×1968；上游 T0 的 rotation 仍 ambiguous、current
size/orientation 仍 unknown。两者职责不同，不能把 C1a 的当前显示读数追溯写成 T0 orientation 已通过。

## Diagnostic 结果与能力边界

validation 的 `diagnostic_observed=false`、`diagnostic_status=blocked`，以下七项 reason 原样保留：

```text
window_pane_bijection_invalid
target_window_pane_missing
node_binding_invalid
target_title_not_unique
region_candidate_missing
focus_fallback_insufficient
focus_target_conflict
```

两帧都已有 aw1→ap1、aw2→ap2 的 exact-bounds `root_subtree` 几何绑定；但两个 pane role 都是 `unknown`，
因此不能建立合同要求的“一 navigation + 一 conversation”语义双射。与此同时，目标 pane/title、有效
node/region 与可信 focus 绑定均未成立：`an1`/`an2` ownership label 正确但 bounds 都是 `[0,0,0,0]`；
title 与 toolbar/message/input 候选数都是 0；`aw2` 虽为 focused application window，却没有 focused input
candidate，故 focus 必须保持 `unknown`。这些只说明 a11y 证据没有产生候选，不能写成屏幕上物理不存在标题、
消息区或输入框。IME 为 hidden/none/not_active，本轮没有因此新增 blocker。成功 sidecar 没有删除或掩盖任何一项。

本 run 的能力结论固定为：

- `runtime_evidence=false`；
- `layout_accepted=false`；
- `wechat_layout_verified=false`；
- `editor_action_ready=false`；
- `settings_mutation_allowed=false`；
- `device_action_allowed=false`；
- `p0_capability=unsupported`；
- `execution_grant=false`。

因此只能写“C1a trusted origin/read-only 取证成功”，不能写“T-L1 通过”。app 仍未合 main，T-L2 与任何危险
动作继续禁止；下一步是 A3/C1b 根据本次真实 window/root/node/region/focus 形态冻结新 contract、对抗 fixture
与 validator。

## Ledger 边界

本轮使用 direct C1a runner，sidecar 明确 `dispatch_used=false`。该入口按合同不写 `docs/runs/ledger.csv`；
未新增 ledger 行不是遗漏，也不得人工补造。固定 SHA、五文件 evidence 与本记录共同承担本 run 的归因。
