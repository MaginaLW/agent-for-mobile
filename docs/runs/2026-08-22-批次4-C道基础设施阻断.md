# 2026-08-22 · 批次 4 C 道基础设施阻断

候选固定为 `636048a6359f9ebae71a7ceb8a551fc1b2ca6b72`。原固定 C task
`019ffbc0-f5a7-7701-9a86-0bf3d07242bf` 与 worktree 已不在当前 Codex 主机，因此开跑前冻结；
未复用任何旧 run 或证据。

## Run 1 · ADB 路径未接入 runner

- run：`20260822T203023-11ee0a8b00c6`
- 结果：setup fail，`设备发现 失败。`
- 根因：runner 默认使用裸 `adb`，PATH 无 platform-tools；`app/local.properties` 不参与 runner 的
  ADB 发现。绝对路径执行 `adb devices` 与 `get-state` 均正常，排除设备掉线。
- 证据：`legs=[]`、`cleanup.ok=true`；未生成 slug、未启动 dispatch，故 ledger 无行。

## Run 2 · vivo 系统安装确认未处理

- run：`20260822T203737-ef7fb2ce2896`
- 结果：setup fail，`安装 debug APK 超时。`
- 根因：`adb install -r` 发起后，logcat 于 20:37:39 记录
  `com.android.packageinstaller/.PackageInterceptActivity`；无人点击系统安装确认，120 秒后 runner
  按硬上限终止。设备仍在线，旧包 `lastUpdateTime=2026-08-13 20:12:25`，安装未完成。
- 证据：`legs=[]`、`cleanup.ok=true`；无 trace、确认截图、slug 或 ledger 行。

两条 run 都没有进入 Allow，四腿全部 NOT RUN；批次 4 仍为 0/4、未判定。下一条必须使用全新
clean C，显式传绝对 `-AdbPath`，并在 Provision 开始后由用户立即确认 vivo 系统安装页。

## Run 3 · 新旧 debug 签名不一致

- run：`20260822T204640-c6c54d3583e4`
- 结果：用户守在系统安装页并点击后约 11 秒返回 `安装 debug APK 失败。`
- 证据：`legs=[]`、`cleanup.ok=true`、无 ledger；设备旧包 `lastUpdateTime` 仍为 08-13。
- 根因：只读 pull 手机上的 `base.apk` 后，以 SDK 35 `apksigner` 比对证书，旧包 SHA-256 为
  `18b545effb641f1e69c1aa25a2dd99717deede41dea883f8159ff61e93924809`，新包为
  `ef99efaa844c7c910eafb16eb164ad14594abc7248f73d9a48b1ebeb39969ea1`。本机唯一
  `~/.android/debug.keystore` 创建于当日 20:27，旧 key 不在 C/D 盘。

因此下一步不是再跑安装参数，而是由用户在“恢复旧 key”与“卸载旧网关后重装”之间作决定。
后者会清除网关 App 私有数据及授权，但不影响微信；未经明确授权不得执行。

## Run 4 · 新包装好后 dispatch 子层仍找不到 adb

- 用户明确授权后，只卸载了已核对包名的 `dev.magina.gateway`；微信未受影响。新 debug APK 随后
  安装成功。
- run：`20260822T205229-53a0760161f9`
- 结果：Allow 在真人确认前以 `预检失败：adb 不在 PATH` fail-closed；Stale/Deny/Reentry NOT RUN。
- 证据：manifest 仅一条 Allow，`human_decision=not_observed`、teardown clean、cleanup true；ledger
  恰好一行。根因是 runner 的绝对 `-AdbPath` 没有覆盖 dispatch 子层的裸 `adb` 检查。

## Run 5 · 旧候选硬钉 Codex 0.147

- run：`20260822T205622-e75f7a2242a0`
- 结果：同一 runner 进程已补齐 SDK PATH，绝对 adb 正对照通过，Provision 也成功；Allow 仍在真人
  确认前 fail-closed，Stale/Deny/Reentry NOT RUN。
- 证据：当前官方签名 CLI 报 `codex-cli 0.149.0-alpha.4.1`，旧候选明确抛出“版本不符合已验证的
  0.147 JSONL 契约”；manifest 记录 `human_decision=not_observed`、teardown clean、cleanup true，
  ledger 恰好一行。

两条 run 均按规则冻结、不复用。A 道新候选固定为
`de6685c65b3ea3fe71bc41c95802791e69f49460`：精确兼容已验证的 0.149 契约，真实签名 CLI smoke
与 15 条 Codex 离线契约均通过。批次 4 仍为 0/4；下一条必须基于该 SHA 建全新 clean C。

## Run 6 · 新候选再次停在 vivo 安装确认页

- 候选：`de6685c65b3ea3fe71bc41c95802791e69f49460`
- run：`20260822T231712-63fc96bdce88`
- 结果：本 worktree 唯一实际构建成功；runner 启动后设备在线，但 120 秒内始终停在
  `com.android.packageinstaller/.PackageInterceptActivity`，包的 `lastUpdateTime` 未变化，最终以
  `安装 debug APK 超时。` 在 setup 失败。
- 证据：`legs=[]`、`cleanup.ok=true`，无 slug/dispatch，故无 ledger；持久 manifest SHA-256 为
  `6E9D8EDAB3F1CEF8F77A529E16C62C8DB504347D53A3D9F4D686A75C66B9BA55`。

该 run/worktree 已冻结，不得重跑。批次仍为 0/4；下一条必须重新建立 clean C，并在开跑前确认用户
确实看得到系统安装页、能在 runner 的安装窗口内完成点击。

## Run 7 · precheck 与生产标题判据分叉

- 候选：`de6685c65b3ea3fe71bc41c95802791e69f49460`
- run：`20260822T232308-85e978e72f99`
- 结果：唯一构建与安装成功；Allow 只调用一次
  `macro_run(p0_wechat_file_transfer_prepare)`，在真人确认前以
  `E_BLOCKED stage=unrecognized_entry` fail-closed。未输入、未发送；Stale/Deny/Reentry NOT RUN。
- 证据：`human_decision=not_observed`、teardown clean、cleanup true；ledger 恰好一行。持久目录共 5 个
  文件（manifest、trace、stdout/stderr、失败截图），截图明确在文件传输助手且输入框为空。

截图并非宏失败时的同一 OCR 数据帧，本 run 又未保存 OCR candidates，故不能断言具体读成了尾字还是
漏识；但代码根因已机械坐实：precheck 的 `P0FocusProbeValidator.build` 仍用旧 `contains` 并从候选中
直接挑目标标题，宏/生产已改用 `ConversationSurfacePolicy` 先结构选标题再精确匹配。同一 snapshot 可
出现 precheck `probe_ready=true`、macro `unrecognized_entry`。安全门拒绝是正确的；A 道应统一判据并
持久化 precheck 证据，禁止放宽生产标签匹配。该候选与 run/worktree 均冻结。
