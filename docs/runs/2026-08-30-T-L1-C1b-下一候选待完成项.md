# T-L1 C1b 下一候选待完成项与闭合判据

## 当前基线

- 最近已消费且不得重跑的 build-only one-shot 为
  `a661f365322bbb8ef3aa83f7a84b9bcb23a51f6e`；此前六个失败 SHA 同样永久冻结。
- r13 read-only preflight 已闭合：leaf `bf15d009…fcd57c` 外部 exit `0`、terminal closed、三类 failure `0`，三阶段
  ADB/5037 都为 `0/0`；receipt `55524bc8…67b010` 不是后续执行 authority。
- `a661f36` helper 在 Gradle 前命中 Git for Windows file count 从冻结 `9576` 漂移成 `9577`；退出后独立只读
  重算才确认当前 catalog `deeaa…34ee` 也不同于冻结 `4c5e…7458`。只在内存排除 38-byte `etc/mtab` 后 catalog
  仍为 `556e…f838`，不能只删文件或改 count。
- helper 已生成合法 closed failed summary，stdout 与 summary bytes + CRLF 的长度/hash 均 exact；launcher 的
  `Read-LauncherHeldFileBytes` 却用 `return $bytes`，返回值被 PowerShell 展开为 `Object[]`，随后在
  `Buffer.BlockCopy` 确定性失败并遮蔽 Git primary。该 P1 对 passed/failed summary 都必现。
- 四件失败证据已 ordinary/non-reparse 验证并设为 ReadOnly；module `build`、workspace、journal、module Gradle、
  `local.properties` 与 launcher/helper 进程均无残留，ADB/5037 退出后边界快照仍为 `0/0`。外层 exit `1`、
  launcher/helper start=`1/1`、exit=`1/1`、retry=`0`、cleanup failure=`0`；完整 hashes 见
  [冻结失败记录](2026-09-01-T-L1-C1b-a661f36-real-build-smoke失败.md)。
- r10 helper/launcher、r13 leaf/receipt 与本轮输出都只作历史证据，不编辑、不复用授权。新链必须用新 clean commit
  short 与新 revision，避免 commit-short 派生的 `.checks` 路径和 failure sidecar 与历史证据冲突。
- 形成本文档本次更新时的输入基线为 `a661f365322bbb8ef3aa83f7a84b9bcb23a51f6e`；本文档提交后 HEAD 会变化，
  因此不得直接把 `a661f36` 当作下一次 preflight/smoke 候选。

## 有序待完成项

以下各项必须按顺序闭合；某项失败时停止，不跳项、不自动重试：

1. 以冻结的 `launcher-template-r10.ps1` 为唯一输入新建 `launcher-template-r11.ps1`，只把
   `Read-LauncherHeldFileBytes` 的唯一成功返回从 `return $bytes` 改为 `return ,$bytes`；r10 保持只读不改。
2. 用固定 PowerShell 7.6.4 做静态与隔离 runtime canary：唯一 return 必须是 unary-comma 语法对应的单元素
   `ArrayLiteralAst`（不是 `UnaryExpressionAst`）；普通 return、cast、
   `@()` mutation 必须拒绝；1 byte、多 byte、passed/closed-failed bytes 均返回 exact `System.Byte[]`，hash 不变且
   `BlockCopy` + 唯一 CRLF 成功。failed summary 必须在 generic exit/stderr 前公开 reasons，passed 仍只由 verifier 接受。
3. 为本文档提交后的新完整 SHA 建 `render-final-r12-<short>.ps1`，生成 helper/launcher r11。renderer 对新 template、
   历史 r10 pair 与 authority inputs 保留 no-follow held handle、stable ID、handle-derived final path；temp durable write/
   exact readback/ReadOnly 与 final bridge/guard 持有到整套结果闭合，目录 mtime equality 仍不是门禁。
4. 建 `render-preflight-r14-<short>.ps1` 与 r14 leaf：保留 r13 的三阶段固定 BCL ADB/5037 零边界、module/output
   no-follow absence、Git 四调用、stable-ID/final-path/held 门；新增 launcher unary-comma/runtime canary。
5. r14 preflight 还须以与 build guard 同等冻结规则只读核验 `${PROGRAMFILES}/Git` 的 file count、catalog 与已固定的
   identity/hardlink topology；这项新增 trust-tree 核验本身不执行 `git.exe`，也不启动 build，并且必须先于 r13
   已有的四次 repo 只读 Git 检查。环境漂移必须在申请 one-shot 前 fail-closed；只有 trust-tree 通过后，四次
   repo 只读 Git 检查才继续运行。
6. Git 安装恢复/重装或安全重基线属于主机变更，须另行明确授权。恢复路径优先使用 Git for Windows
   [官方 immutable release `v2.55.0.windows.3`](https://github.com/git-for-windows/git/releases/tag/v2.55.0.windows.3)：
   x64 setup `Git-2.55.0.3-64-bit.exe` 的已发布 SHA-256 是
   `af12577d0fdff74243a5988197aa49b957d5044edc17004f6ddf0768996f1dca`；下载后仍须独立复核 SHA 与 Authenticode，
   并证明全树回到 `9576 / 4c5e…7458`。若干净安装仍不同，必须保存逐路径 manifest 并审计来源、
   key hashes、identity/hardlink topology 后再同步常量/schema/fixture/文档，不能只接受当前 digest。
7. 环境恢复后再次被动确认 ADB/5037 为 `0/0`；固定新 clean HEAD、raw index 与 exact external artifacts，完成独立
   P0/P1/P2 静态复核，然后只运行一次 r14 read-only preflight。它不得启动 launcher/helper/Gradle/JDK/ADB 或访问设备。
8. preflight 闭合后冻结工件与 receipt；其后不得运行会重建 module `build`、改变 Git trust root 或重启 default ADB
   的工具。只有用户针对新完整 SHA 明确授权后，才能启动一次 launcher；automatic retry 固定 `0`，旧授权不可复用。

## 仍未授权的范围

本清单不授权 install、ADB 命令、设备发现、T0、C1b 采集、截图、OCR、目标 App 启动、系统设置修改、关闭手机套件或
终止宿主进程，亦不放行
navigation、conversation、target、regions、layout、微信布局、editor action、T-L1、P0 或 execution。

## 后续记录要求

- 新 preflight leaf 只写 repo-external receipt 内部字段；进程结束后由外层独立计算 leaf/receipt SHA，并观察外部 exit、
  terminal state 与 primary/cleanup/recording failure count。receipt 不能自记自身最终 SHA 或尚未发生的进程 exit；
  两类证据必须联合消费后才能判 `prepared_not_authorized`。
- build-only one-shot 无论通过或失败都另建冻结 run 文档；记录 launcher/helper start 与 exit、retry、Gradle/签名/
  held Git/ADB/设备计数、正式输出与 failure-only sidecar 的 bytes/hash/ReadOnly 状态。
- 若 one-shot 失败，同一 SHA 不重跑；先冻结证据、更新本册与 backlog，再形成新 SHA。
