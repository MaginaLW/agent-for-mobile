# T-L1 C1b 下一候选待完成项与闭合判据

## 当前基线

- 已消费且不得重跑的 build-only one-shot 为
  `21d29866a428f49e6ea79fe7fedc56f6cf42e16e`；它在 Gradle 前被既有
  `app/tablet-c1b-probe/build` 阻断，整体失败。
- r7 read-only preflight 已闭合；其 leaf 与 receipt 仅是 `21d2986` 的历史证据，不能为后续 SHA 授权。
- 旧 build 树已同卷移至 `${QUARANTINE_ROOT}`，原 module output 路径 absent；隔离树可恢复，不在本清单中删除。
- `21d2986` 的 helper primary 实际存在于 closed failed summary，但 launcher 的跨行 `[long]` 写法和消费顺序令
  failure-only sidecar 先记录了 generic closure。该轮没有 false-pass，但诊断真相没有闭合。
- 形成本文档时的输入基线为 `abbae235153d63b5ff8e6ee8ff6421df655c8ce4`；本文档提交后 HEAD 会变化，
  因此不得直接把该输入 SHA 当作下一次 smoke 候选。

## 有序待完成项

以下各项必须按顺序闭合；某项失败时停止，不跳项、不自动重试：

1. 在 repo-external launcher 模板中把 active-process count 固定为含唯一 native call 的单一 cast RHS；
   validation-time 与 cleanup-time 各自保存 Job active count 和 child-termination snapshot，cleanup 不得覆盖 validation。
2. helper process/job/drain 闭合后，无论 exit code 都先以 no-follow held handle 绑定 summary，核对 stdout 为
   `summary bytes + CRLF` 及全流 SHA，再用已捕获的 exact verifier `FunctionInfo` 做 strict UTF-8、duplicate-key、
   Int64 与 closed-property 解析。
3. `status=failed` 时使用独立的 closed failure validator 绑定 commit/helper、执行包络、计数/布尔类型、core
   all-null/all-present、cleanup state 与 `failure_count/reasons`；验证后的 helper reasons 是 primary，exit/stderr 只作
   并列元数据。helper 的 `status=passed` 仍只能由现有 public verifier 接受，失败 validator 不能产生 pass authority；
   launcher 自写 result 只能标为需外部 exit 的 candidate，不能在自身结束前声称 final passed。producer 须在
   64 KiB summary 合同内对 observer/cleanup 数量和单条文本作确定性界限，省略说明不得删除 primary/core。
4. 静态门用 AST 机械证明第 1–3 项，并确认 stable ID、no-reparse、final path、完整 held ancestor chain、leaf handle、
   文件 hash/size/mtime 与 start=`1`、retry=`0` 均未弱化；目录 mtime equality 继续不是门禁。observer end 必须在
   实际 unsubscribe 成功点记录，post-boundary/residue 任一观测未完成都只能 fail closed。
5. 新 read-only preflight 以 no-follow 检查 module `build` 在初始、primary guard 释放前和 final pre-receipt
   三个观察点均 absent；
   同时继续要求 summary/log/result/failure sidecar 不预存。该检查不声称冻结 preflight 到 smoke 之间的 namespace。
6. 文档提交后固定新的完整 clean HEAD、raw index SHA 和 exact repo-external helper/launcher/preflight；完成独立静态复核，
   然后只运行一次 read-only preflight。它不得启动 launcher/helper/Gradle/JDK/ADB，不得访问设备。
7. preflight 闭合后冻结工件与 receipt；在 build-only one-shot 前不得运行会重建 module `build` 的 Gradle/check。
8. 只有用户针对第 6 项的完整 SHA 明确授权后，才能启动一次 launcher；automatic retry 固定为 `0`。旧 SHA、旧
   preflight 或旧授权均不可复用。

## 仍未授权的范围

本清单不授权 install、ADB、设备发现、T0、C1b 采集、截图、OCR、目标 App 启动、系统设置修改，亦不放行
navigation、conversation、target、regions、layout、微信布局、editor action、T-L1、P0 或 execution。

## 后续记录要求

- 新 preflight leaf 只写 repo-external receipt 内部字段；进程结束后由外层独立计算 leaf/receipt SHA，并观察外部 exit、
  terminal state 与 primary/cleanup/recording failure count。receipt 不能自记自身最终 SHA 或尚未发生的进程 exit；
  两类证据必须联合消费后才能判 `prepared_not_authorized`。
- build-only one-shot 无论通过或失败都另建冻结 run 文档；记录 launcher/helper start 与 exit、retry、Gradle/签名/
  held Git/ADB/设备计数、正式输出与 failure-only sidecar 的 bytes/hash/ReadOnly 状态。
- 若 one-shot 失败，同一 SHA 不重跑；先冻结证据、更新本册与 backlog，再形成新 SHA。
