# T-L1 C1b 下一候选待完成项与闭合判据

## 当前基线

- 最近已消费且不得重跑的 build-only one-shot 为
  `690693ae4113a91f7590457a888b56e93b6e200b`；它在 Gradle 前被宿主已有的 1 个 `adb.exe` 与 1 个 TCP/5037
  listener 阻断，整体失败。此前五个失败 SHA 同样永久冻结。
- r11 read-only preflight 已闭合；leaf `371b32be…a1f6` 与 receipt `be20bd9a…a2d0` 仅是 `690693a` 的历史证据，
  不能为后续 SHA 授权。
- `690693a` 的 helper 已生成合法 closed failed summary，且 stdout 与 summary bytes + CRLF 精确一致；launcher 却把
  `-ExpectedSha256 $null` 绑定成空串并误触 canonical-hash 门，使 failure-only sidecar 再次遮蔽 helper primary。
- 本轮四件失败证据已只读冻结，module `build`、`.gradle`、`local.properties`、fresh workspace 与 journal 均无残留。
- 宿主手机套件的 `adb.exe` / TCP/5037 listener 仍存在；本轮没有终止或修改它。不得把“已定位”写成“环境已归零”。
- 形成本文档本次更新时的输入基线为 `690693ae4113a91f7590457a888b56e93b6e200b`；本文档提交后 HEAD 会变化，
  因此不得直接把该输入 SHA 当作下一次 smoke 候选。
- optional expected-SHA 的两处 nonempty 修复已在冻结的 launcher template 上静态通过：只改 canonical check 与
  final compare 的存在性判断，stable ID、no-reparse、最终路径、held handle、实际 hashing 与 failed-summary 消费顺序均未放宽。
- 首个 exact-pair renderer `76f63fbf…a8e49` 因输入和发布阶段缺少完整 stable-ID/final-path/ancestor-held 绑定而
  rejected-static；它从未执行，新的 helper/launcher leaf 仍不存在。
- 首个 passive-host preflight renderer `5f623d34…e48e1` 也 rejected-static 且从未执行。其预测 leaf 把
  `$reparseFlag` 从错误的 launcher 函数取证，形成确定性假拒；新增 CIM/NetTCPIP 命令还可能经可变
  `PSModulePath` 自动加载同名模块，不能作为宿主零边界的权威观察。预测 leaf 从未写入或运行。

## 有序待完成项

以下各项必须按顺序闭合；某项失败时停止，不跳项、不自动重试：

1. 在新的 repo-external launcher leaf 中修正 `Open-LauncherHeldExactFile` 的 optional expected SHA：canonical check 与
   final compare 都只能在 nonempty expected hash 时执行。null/empty 仍必须完成 no-follow held open、stable ID、最终路径、
   length/mtime 与实际 SHA 计算，不能成为弱化文件门禁的旁路。
2. 用固定 PowerShell 7.6.4 的正/负对照证明参数绑定语义：无 pin 的刚生成 summary 能 held-bind；非空非法 hash
   必须拒绝；固定输入的合法 hash 必须 exact compare。静态门须锁住调用点传 `$null` 的语义与两处 nonempty 判定。
3. helper process/job/drain 闭合后，无论 exit code 都先绑定 summary、核对 stdout 为 `summary bytes + CRLF` 与全流 SHA，
   再用已捕获的 exact verifier `FunctionInfo` 做 strict UTF-8、duplicate-key、Int64 与 closed-property 解析；本轮 valid
   failed summary 必须作为固定正对照进入独立 failure validator，而不能在 held-open 前被拒绝。
4. `status=failed` 的验证结果继续绑定 commit/helper、执行包络、计数/布尔类型、core all-null/all-present、cleanup state
   与 `failure_count/reasons`；验证后的 helper primary/reasons 优先，exit/stderr 只作并列元数据。passed summary 仍只能由
   public verifier 接受，failure validator 与 launcher 自写 result 都不能单独产生 pass authority。
5. 静态门继续证明单一 cast RHS、validation/cleanup 双快照、stable ID、no-reparse、final path、完整 held ancestor chain、
   leaf handle、hash/size/mtime、start=`1` 与 retry=`0` 未弱化；目录 mtime equality 继续不是门禁。observer end 与
   post-boundary/residue 的既有失败真值合同保持不变。
6. exact-pair renderer 必须对八个输入和祖先链保留 no-follow held handle、stable ID 与 handle-derived final path；两份
   temp 在发布前完成同柄写入、durable flush、精确回读和只读冻结，发布后以同一 stable ID 桥接到 deny-write/delete
   final guard，并持有到两件工件和成功结果全部闭合。目录时间戳等值继续不是门禁；失败不得把半套残留写成成功。
7. 新 read-only preflight 除 module `build` 与四个正式/失败输出的多点 no-follow absence 外，再以固定 BCL/native
   只读 API 分三阶段观察宿主 `adb.exe` process count 与 TCP/5037 listener count，并要求六个计数均为 `0`。不得依赖
   可变模块搜索路径自动加载 CIM/NetTCPIP 命令；不得运行 adb、枚举设备或终止进程，观察不可用也必须 fail closed。
   receipt 同时保留通用与专项的 termination-attempt=`0` 字段，避免既有消费者因字段改名失去闭包。
8. 新 preflight 的 launcher AST 门必须从唯一 `Assert-LauncherFileIdentity` 取证 `$reparseFlag`，同时证明 held-binding
   对 identity/no-reparse 门的调用未减少；不能把静态门自身写成确定性假拒。preflight renderer 的发布过程同样必须
   具备 stable-ID、no-reparse、最终路径与 held-handle 绑定，不能沿用 close-handle 后按路径 Move/冻结的旧实现。
9. 当前手机套件进程必须由用户关闭，或由用户另行明确授权精确的宿主进程处置；随后用独立被动观察确认
   adb/listener 均为零。不得把 build-only 授权扩张成关闭手机套件或 kill 未知进程的授权。
10. 文档提交后固定新的完整 clean HEAD、raw index SHA 与 exact repo-external helper/launcher/preflight；完成独立静态
    复核，然后只运行一次 read-only preflight。它不得启动 launcher/helper/Gradle/JDK/ADB，不得访问设备。
11. preflight 闭合后冻结工件与 receipt；其后不得运行会重建 module `build` 或重启 default ADB 的工具。只有用户针对
    新完整 SHA 明确授权后，才能启动一次 launcher；automatic retry 固定为 `0`，旧 SHA/leaf/receipt/授权均不可复用。

## 仍未授权的范围

本清单不授权 install、ADB 命令、设备发现、T0、C1b 采集、截图、OCR、目标 App 启动、系统设置修改、关闭手机套件或
终止宿主进程，亦不放行
navigation、conversation、target、regions、layout、微信布局、editor action、T-L1、P0 或 execution。

## 后续记录要求

- 新 preflight leaf 只写 repo-external receipt 内部字段；进程结束后由外层独立计算 leaf/receipt SHA，并观察外部 exit、
  terminal state 与 primary/cleanup/recording failure count。receipt 不能自记自身最终 SHA 或尚未发生的进程 exit；
  两类证据必须联合消费后才能判 `prepared_not_authorized`。
- build-only one-shot 无论通过或失败都另建冻结 run 文档；记录 launcher/helper start 与 exit、retry、Gradle/签名/
  held Git/ADB/设备计数、正式输出与 failure-only sidecar 的 bytes/hash/ReadOnly 状态。
- 若 one-shot 失败，同一 SHA 不重跑；先冻结证据、更新本册与 backlog，再形成新 SHA。
