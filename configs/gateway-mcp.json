{
  "_comment": "M1 自研执行网关的大脑侧挂载配置。前置：手机端网关服务已启动 + `adb forward tcp:8848 tcp:8848`。token 在网关 App 主界面（点「复制 token」），替换下方占位符后使用；本文件含真实 token 时不要提交。",
  "mcpServers": {
    "gateway": {
      "type": "http",
      "url": "http://127.0.0.1:8848/mcp",
      "headers": {
        "Authorization": "Bearer 70a1013ae5d54625879cc00d79fb614f"
      }
    }
  }
}
